#!/usr/bin/env node
// چکِ ساختارِ خوانشِ v4 (bots/tarot/STYLE.md).
//
// این ساختار از تقاطعِ دو چیز درآمده: تحلیلِ خوانش‌های واقعیِ یک تاروت‌خوانِ انسان، و دو
// تحقیقِ مستقل درباره‌ی اینکه چه چیزی در خروجی باعث بازگشتِ کاربر می‌شود. هرچه در **هر
// دو** بوده وزنِ بالا گرفته و اینجا قفل شده، تا در بازنویسی‌های بعدی بی‌صدا از بین نرود.
import fs from 'node:fs';

const SRC = fs.readFileSync(new URL('../bots/tarot/index.js', import.meta.url), 'utf8');
// هسته‌ی خالصِ خوانش. از v3.6.0 ساختِ کانتکست، رندرِ متنِ نهایی و هلپرهای متن این‌جا
// هستند تا `tools/reading-lab.mjs` بتواند **همان کد** را آفلاین اجرا کند. ادعاهایی که
// قبلاً روی index.js بودند، حالا این‌جا را می‌بینند؛ نیتشان عوض نشده، هدفشان جابه‌جا شده.
const CORE = fs.readFileSync(new URL('../bots/tarot/reading-core.js', import.meta.url), 'utf8');
// و چون هسته یک ماژولِ واقعیِ importable است، دیگر لازم نیست منطق را از متنِ سورس
// بیرون بکشیم: خودِ تابعِ صادرشده اجرا می‌شود (دقیق‌تر از new Function روی slice).
const core = await import('../bots/tarot/reading-core.js');
const LOC = fs.readFileSync(new URL('../bots/tarot/locales/fa.js', import.meta.url), 'utf8');
const V4 = LOC.slice(LOC.indexOf('readerSystemV4'), LOC.indexOf('readerSystem: (spread)'));

let pass = 0; const errs = [];
const ok = (c, m) => { if (c) { pass++; console.log(`  ✅ ${m}`); } else { errs.push(m); console.log(`  ❌ ${m}`); } };

console.log('▶ دامنه و رول‌بک (بند ۲ج-۲ ریشه)');
{
  ok(/const READING_V4 = true;/.test(SRC), 'پرچمِ اصلی روشن است');
  ok(/const READING_V4_ADMIN_ONLY = true;/.test(SRC), 'هنوز فقط ادمین (باز کردن = یک PR جدا)');
  ok(/const v4For = \(uid\) => READING_V4 && \(!READING_V4_ADMIN_ONLY \|\| isAdmin\(uid\)\)/.test(SRC),
    'helper هر دو پرچم را با هم چک می‌کند');
  const raw = [...SRC.split('\n').filter(l => !/^\s*\/\//.test(l)).join('\n')
    .matchAll(/\bREADING_V4\b(?!_ADMIN_ONLY)/g)].length;
  ok(raw === 2, `READING_V4 فقط در تعریف و داخلِ v4For استفاده شده (${raw} مورد)`);
}

console.log('\n▶ سرخط: قانونِ «بله‌ی گران»');
{
  // منطقِ خالص واقعاً اجرا می‌شود، نه فقط وجود داشته باشد
  const V = fs.readFileSync(new URL('../bots/tarot/verdict.js', import.meta.url), 'utf8');
  ok(/export function headlineOk/.test(V), 'اعتبارسنجِ سرخط در ماژولِ خالصِ verdict.js است');
  ok(/headlineOk\(obj\.headline\)/.test(SRC), 'سرخط قبل از پذیرش اعتبارسنجی می‌شود');
  ok(/if \(!headlineOk\(obj\.headline\) && headlineTries\+\+ < HEADLINE_EXTRA_TRIES\) \{ fallback = obj; return false; \}/.test(SRC),
    'سرخطِ بد یک تلاشِ اضافه می‌گیرد، بعد پذیرفته می‌شود (ضدِ ریفاند و ضدِ اسراف)');
  ok(/const HEADLINE_EXTRA_TRIES = 1;/.test(SRC),
    'بودجه‌ی retryِ سرخط محدود است — قبلاً ۵ بار بازتولید می‌شد و آخرش همان سرخط نشان داده می‌شد');
  // فرمول در خودِ پرامپت
  ok(/\[جهت\] \+ \[قیدِ احتمال\] \+ «اما\/ولی» \+ \[بهای مشخص\]/.test(V4),
    'فرمولِ سرخط صریح در پرامپت آمده');
  ok(/نهِ خشک و بی‌راه‌حل|هرگز نهِ خشک/.test(V4), 'نهِ بی‌راه‌حل ممنوع است');
  ok(/خوش‌بینیِ توخالی ممنوع|دروغِ شیرین/.test(V4),
    'خوش‌بینیِ کاذب ممنوع است (هر دو تحقیق: sugarcoating اعتماد را می‌خورد)');
}

console.log('\n▶ چهار حرکتِ وزن‌بالا (هم در تحقیق‌ها، هم در خوانشِ واقعی)');
{
  ok(/حسِ ناگفته را نام ببر/.test(V4),
    'نام‌گذاریِ حسِ ناگفته — قوی‌ترین محرکِ بازگشت در هر دو تحقیق');
  ok(/الگوی بین‌کارتی را قبل از تک‌تکِ کارت‌ها/.test(V4),
    'الگوی بین‌کارتی قبل از کارت‌به‌کارت (تحقیق: «سنتز» چیزی است که حرفه‌ای را از آماتور جدا می‌کند)');
  ok(/تصویرِ روی کارت را توصیف کن/.test(V4),
    'توصیفِ تصویرِ کارت (تحقیق: فرض نکن مخاطب معنی کارت‌ها را می‌داند)');
  ok(/کارتِ درباری را به یک آدمِ واقعی/.test(V4), 'کارتِ درباری به آدمِ واقعی ترجمه می‌شود');
  // «کارتِ نیامده» حذف شد (۱۴۰۵/۰۵/۲۷). دو نسل تلاش شد و اندازه‌گیریِ دورِ چهارم گفت
  // مدل فقط ۲ از ۶ بار کارتِ داده‌شده را می‌آورد و دو بار کارتی گفت که در دستِ ۷۸تایی
  // **وجود ندارد**. طبق بند ۹/۰ ریشه لایه‌ی سومِ وصله نوشته نشد؛ خودِ فیلد رفت.
  // این ردیف قفل می‌کند که برنگردد بی‌آنکه تصمیمِ صریح گرفته شود.
  ok(!/کارتِ سنگینی که نیامده/.test(V4), 'فیلدِ «کارتِ نیامده» از پرامپت حذف شده');
  ok(!/export function absentHeavy/.test(CORE), 'کدِ مرده‌ی انتخابِ کارتِ نیامده پاک شده');
  ok(core.absentHeavy === undefined, 'absentHeavy دیگر export نمی‌شود');
  // جایگزینش: قانونِ لنگر — همان چیزی که آزمایشگاه مکانیکی می‌سنجد (`anchorScore`).
  ok(/قانونِ لنگر/.test(V4), 'قانونِ لنگر در پرامپت هست');
  ok(/نامِ یکی از کارت‌های همین فال/.test(V4) && /یک کلمه از خودِ سؤالِ او/.test(V4)
    && /چیزی از شناختِ قبلی/.test(V4), 'هر سه لنگر صریح نام برده شده‌اند');
}

console.log('\n▶ چیزهایی که عمداً **رد** شدند (تناقضِ تحقیق با نمونه یا با تصمیمِ مالک)');
{
  // تحقیق دوم واژگانِ «کائنات/شهود» را پیشنهاد می‌دهد؛ نمونه‌ها هرگز از آن استفاده
  // نمی‌کنند و قاعده‌ی «هر جمله باید جهت داشته باشد» آن را رد می‌کند.
  ok(/«کائنات»/.test(V4) && /ممنوع/.test(V4), '«کائنات» و «به شهودت اعتماد کن» ممنوع مانده‌اند');
  // تحقیق دوم می‌گوید از جوابِ بله/خیر طفره برو؛ خودش هم می‌گوید طفره‌رفتن بازخوردِ منفی
  // می‌گیرد. نمونه‌ها جوابِ صریح می‌دهند، پس صراحت برنده است.
  ok(/طفره رفتن بدترین کاری است/.test(V4), 'طفره‌رفتن صریحاً ممنوع است، نه توصیه‌شده');
  // لیستِ قدم عملی در v3.2.0 حذف شد و نمونه‌ها نصیحت را داخلِ جمله می‌آورند
  ok(/همین‌جا داخلِ جمله بیاید نه به‌صورت لیست/.test(V4), 'نصیحت داخلِ متن است، نه لیستِ قدم عملی');
  ok(!/action_items/.test(V4), 'فیلدِ action_items در v4 وجود ندارد');
  // وابستگی‌سازی: خط قرمزِ صریحِ هر دو تحقیق
  ok(/وابسته کند/.test(V4), 'وابسته‌کردنِ مخاطب ممنوع است (خط قرمزِ هر دو تحقیق)');
}

console.log('\n▶ شکلِ خروجی');
{
  ok(/"teaser"/.test(V4) && /جوابِ نهایی را \*\*لو نده\*\*/.test(V4),
    'مرحله‌ی افشا فقط تیزر دارد و جواب را لو نمی‌دهد');
  // تیزر باید واقعاً درباره‌ی **خودِ کارت** باشد: نام + معنی + تصویر. بازخوردِ میدانی:
  // نسلِ اول تیزرها تفسیر بودند نه معرفیِ کارت، و کاربر ابزاری برای رمزگشایی نداشت.
  ok(/نامِ کارت را بیاور/.test(V4), 'تیزر نامِ کارت را می‌آورد');
  ok(/تصویرِ روی کارت\*\*/.test(V4), 'تیزر یک جمله از تصویرِ روی کارت دارد');
  ok(/ابزار بده تا خودش کم‌کم قصه را بچیند/.test(V4), 'هدفِ تیزر: کاربر خودش رمزگشایی کند');
  ok(/v4 \? llm\.cards\[idx\]\?\.teaser : llm\.cards\[idx\]\?\.text/.test(SRC),
    'در افشا تیزر نمایش داده می‌شود، نه تحلیلِ کامل');
  ok(/const askFeedback = !v4 &&/.test(SRC), 'هیچ بازخوردی وسطِ خوانش گرفته نمی‌شود');
  ok(!/confirmation_question/.test(V4), 'سؤالِ تأییدیِ وسطِ خوانش از پرامپتِ v4 حذف شده');
  ok(/if \(headline\) await ctx\.reply\(headline\);/.test(SRC), 'متنِ نهایی با سرخط شروع می‌شود');
  // ترتیبِ **ارسال** در ربات: سرخط، بعد بدنه، بعد جمع‌بندی
  const fin = SRC.slice(SRC.indexOf('async function finishReading('));
  const iH = fin.indexOf('if (headline)'), iB = fin.indexOf('if (body)'), iC = fin.indexOf('if (closing)');
  ok(iH > 0 && iB > iH && iC > iB, 'ترتیبِ ارسال: سرخط → بدنه → جمع‌بندی');
  // و ترتیبِ **ساخت** در هسته: سرخط قبل از الگو، الگو قبل از جمع‌بندی
  const iCh = CORE.indexOf('llm.callback'), iP = CORE.indexOf('llm.pattern'), iCl = CORE.indexOf('llm.closing');
  ok(iCh > 0 && iP > iCh && iCl > iP, 'ترتیبِ کد در هسته: ارجاع → الگو → جمع‌بندی');
  {
    // رندر واقعاً اجرا می‌شود، نه اینکه فقط وجود داشته باشد
    const r = core.renderV4(
      { headline: 'بله ولی دیر', pattern: 'الگو', reads: [{ text: 'کارت اول می‌گه الف' }, { text: 'ب' }], closing: 'در کل…' },
      [1, 2], ['کارت اول', 'کارت دوم']);
    ok(r.headline.startsWith('🔮 '), 'سرخط نشانه‌ی بخشِ خودش را می‌گیرد');
    ok(r.body.includes('🃏 کارت اول می‌گه الف\nکارت دوم ب'),
      'برچسبِ ترتیبی از کد چسبیده و کارت‌ها در یک بلوکِ پیوسته‌اند');
    ok((r.body.match(/🃏/g) || []).length === 1, 'فقط یک ایموجیِ کارت در کلِ بلوک (نه per کارت)');
    ok(r.closing.startsWith('🕯️ '), 'جمع‌بندی نشانه‌ی بخشِ خودش را می‌گیرد');
  }
  ok(/جایگاه‌ها[\s\S]{0,60}اسمشان را در متن نیاور/.test(V4),
    'نامِ جایگاه به مدل داده می‌شود ولی در متن نمی‌آید');
  ok(/cardLabels: \(n\)/.test(LOC), 'برچسبِ ترتیبیِ کارت‌ها در locale است');
  // برچسب‌ها **شماره‌ای** شدند: نسلِ اول «کارت اولت/بعدیت/آخرت» بود و در فالِ ۱۰کارتی
  // هشت بار پشت‌سرهم «کارت بعدیت» تکرار می‌شد (بازخوردِ میدانی).
  ok(/'اول', 'دوم', 'سوم'/.test(LOC), 'برچسب‌ها شماره‌ی ترتیبی‌اند');
  {
    const m = LOC.match(/cardLabels: \(n\) => ([\s\S]*?\|\| i \+ 1\}`\)\),)/);
    ok(!!m, 'سازنده‌ی برچسب پیدا شد');
    if (m) {
      const f = new Function('return (n) => ' + m[1].replace(/,$/, ''))();
      ok(JSON.stringify(f(3)) === JSON.stringify(['کارت اول', 'کارت دوم', 'کارت سوم']),
        'سه کارت → اول/دوم/سوم');
      const ten = f(10);
      ok(ten.length === 10 && ten[9] === 'کارت دهم', 'ده کارت تا «دهم» شماره می‌خورد');
      ok(new Set(ten).size === 10, 'هیچ برچسبی تکراری نیست (باگِ «کارت بعدیت» ×۸)');
    }
  }
  // شماره‌گذاری کارِ **کد** است نه مدل (قاعده: هرچه کد می‌تواند تضمین کند، در پرامپت نیاید).
  // تاریخچه‌ی دو باگ: اول برچسب دوباره چسبانده می‌شد («کارت آخرت اما کارتِ آخرت»)، بعد
  // فیکسش باعث می‌شد جمله‌ای مثل «این کارت می‌گه…» اصلاً بی‌شماره بماند. حالا قطعی است:
  // هر برچسبی که مدل گذاشته برداشته می‌شود و برچسبِ درست چسبانده می‌شود.
  ok(/return `\$\{labels\[i\]\} \$\{noDash\(stripCardLabel\(t\)\)\}`;/.test(CORE),
    'برچسب همیشه از کد می‌آید (strip + prefix)، نه از مدل');
  // بازخوردِ دورِ سوم: تیترِ ایموجی‌دار per کارت متن را رباتی می‌کرد. خوانشِ واقعیِ انسانی
  // کارت‌ها را پشتِ سرِ هم در یک تکه می‌آورد، نه هر کدام در پاراگرافِ جدا.
  ok(/cardLines\.length \? `\$\{SECT\.card\} \$\{cardLines\.join\('\\n'\)\}` : ''/.test(CORE),
    'خوانشِ کارت‌ها یک بلوکِ پیوسته است، نه یک ایموجی per کارت');
  ok(/\*\*شماره‌ی کارت را ننویس، خودم اضافه می‌کنم\*\*/.test(V4),
    'پرامپت هم می‌گوید شماره را ننویس (تا تکرار اصلاً تولید نشود)');
  ok(!/کارت‌ها را با همین برچسب‌ها صدا بزن/.test(V4),
    'دستورِ برچسب‌گذاری از پرامپت حذف شد (کد ضمانتش می‌کند)');
  {
    // منطقِ برداشتنِ برچسب واقعاً اجرا می‌شود (خودِ تابعِ صادرشده، نه بازسازی از سورس)
    const strip = core.stripCardLabel;
    ok(strip('اما کارتِ آخرت، مرد آویخته…') === 'مرد آویخته…', 'برچسبِ «اما کارتِ آخرت» برداشته می‌شود');
    ok(strip('کارت سوم می‌گه ثبات.') === 'می‌گه ثبات.', 'برچسبِ درستِ مدل هم برداشته می‌شود (ضدِ تکرار)');
    ok(strip('این کارت می‌گه وضعیت روشن نیست.') === 'این کارت می‌گه وضعیت روشن نیست.',
      '«کارت» که برچسب نیست دست‌نخورده می‌ماند (ضدِ جاافتادنِ شماره)');
    ok(strip('نه جامه: رضایتِ عمیقی داری.') === 'نه جامه: رضایتِ عمیقی داری.', 'متنِ بدونِ برچسب دست‌نخورده');
  }
  {
    ok(core.noDash('متن — با تیره') === 'متن، با تیره', 'خط تیره‌ی بلند در کد پاک می‌شود، نه فقط با پرامپت');
    ok([...CORE.matchAll(/noDash\(/g)].length >= 5, 'همه‌ی بخش‌های خروجی از فیلترِ خط تیره رد می‌شوند');
  }
  ok(/بدونِ ایموجی/.test(V4), 'خروجیِ v4 بدونِ ایموجی است (نمونه‌های واقعی هیچ ایموجی ندارند)');
  ok(/بدونِ تیتر و بولد و بولت/.test(V4), 'بدونِ قالب‌بندی');
  ok(!/parse_mode/.test(SRC.slice(SRC.indexOf('if (v4For(uid)) {'), SRC.indexOf('} else {'))),
    'متنِ v4 خام فرستاده می‌شود (نه HTML) — نه escape لازم است نه ریسکِ خرابیِ قالب');
}

console.log('\n▶ درس‌های تستِ میدانیِ ۱۴۰۵/۰۵/۲۶');
{
  // ۱) نشتِ few-shot: جمله‌ی نمونه‌ی «حسِ ناگفته» در ۲ فال از ۳ فال عیناً تکرار شده بود.
  // تحقیق ۱ همین را دلیلِ شماره‌یکِ رهاکردنِ محصولاتِ AI می‌داند: «repetitive, templated».
  // نسخه‌ی قبلی یک **توصیفِ نتیجه** بود («حسِ ناگفته را نام ببر») نه یک رویه، پس مدل
  // نمی‌دانست چطور مشتقش کند و حرفِ خودِ کاربر را با کلماتِ دیگر پس می‌داد. حالا
  // مکانیزم صریح است: فرضِ پنهانی که خودِ سؤال گرفته.
  ok(/سؤالِ او یک چیزی را از پیش فرض گرفته/.test(V4), 'حسِ ناگفته یک رویه دارد، نه فقط توصیف');
  ok(/\*\*حرفِ خودش را پس نده\.\*\*/.test(V4), 'پس‌دادنِ حرفِ کاربر صریحاً ممنوع شد');
  // نشتِ دوم: مثالِ فیلدِ callback هم عیناً کپی شد. **هیچ فیلدی نباید نمونه‌ی
  // قابل‌کپی داشته باشد** — همان اشتباه، فقط در جای جدید.
  ok(!/دفعه‌ی قبل سرِ همین دودلی بودی/.test(V4),
    'مثالِ قابلِ کپی از فیلدِ callback هم حذف شد');
  ok(/با کلماتِ خودت بنویس و از هیچ قالبِ آماده‌ای استفاده نکن/.test(V4),
    'callback صریحاً از قالبِ آماده منع شده');
  ok(!/«انگار ذهنت انقدر روی این که اگر مخالفت کنن چی می‌شه فوکس کرده/.test(V4),
    'جمله‌ی نمونه‌ی قابلِ کپی از پرامپت حذف شده (منبعِ خودِ نشتی)');

  // ۲) لغزشِ ثبت: یک فال «شما/می‌بینید» شد و بقیه «تو». نمونه‌های واقعی ۱۰۰٪ «تو»اند.
  ok(/\*\*همیشه «تو»، هرگز «شما»\.\*\*/.test(V4), 'خطابِ «تو» قفل شده');

  // ۳) حافظه‌ی بین‌جلسه‌ای: رتبه ۲ تحقیق ۱، و نبودش شایع‌ترین دلیلِ رهاکردن. سه فالِ
  // پشت‌سرهم هیچ ارجاعی به هم نداشتند، پس فیلدِ صریح لازم بود نه توصیه‌ی نرم.
  ok(/"callback":/.test(V4), 'فیلدِ صریحِ ارجاع به جلسه‌ی قبل');
  ok(/llm\.callback && `\$\{SECT\.callback\}/.test(CORE), 'ارجاع در خروجی رندر می‌شود');

  // ۴) پنجره‌ی زمانی: رتبه ۳ تحقیق ۱. هیچ‌کدام از سه فال زمان نداشت.
  ok(/بازه‌ی زمانیِ تقریبی بده/.test(V4), 'جمع‌بندی یک بازه‌ی زمانیِ تقریبی می‌دهد');
}

console.log('\n▶ نقشه‌ی راه و کپشنِ ترتیبی');
{
  ok(/flowIntro: \(\)/.test(LOC), 'پیامِ نقشه‌ی راه در locale است');
  ok(/کارت‌هات رو به روی منه/.test(LOC) && /برشون می‌گردونم/.test(LOC), 'متنِ نقشه‌ی راه اصلاح شد');
  ok(/if \(n\) await ctx\.reply\(L\.reading\.flowIntro\(\)\);/.test(SRC), 'نقشه‌ی راه فرستاده می‌شود');
  // ترتیب: نقشه‌ی راه باید **قبل از** پیام‌های انتظار بیاید
  const sr = SRC.slice(SRC.indexOf('async function startReveal('));
  ok(sr.indexOf('flowIntro') > 0 && sr.indexOf('flowIntro') < sr.indexOf('waitLLMWithLoading'),
    'نقشه‌ی راه قبل از پیام‌های انتظار می‌آید');
  ok(/revealCaptionV4:/.test(LOC), 'کپشنِ ترتیبیِ v4 وجود دارد');
  ok(/v4For\(uid\)\s*\?\s*L\.reading\.revealCaptionV4/.test(SRC),
    'کپشنِ عکس در v4 برچسبِ ترتیبی می‌گیرد، نه نامِ جایگاه');
}

console.log('\n▶ دکمه‌ی کارتِ آخر (v3.5.2)');
{
  ok(/finalAnswer: '🔮 حالا جوابم رو بگو'/.test(LOC), 'دکمه‌ی جوابِ نهایی از زبانِ کاربر است');
  ok(/if \(isLast && !v4\) await finishReading/.test(SRC),
    'در v4 جمع‌بندی دیگر خودکار نمی‌آید');
  ok(/`final:\$\{readingId\}`/.test(SRC), 'دکمه readingId را حمل می‌کند');
  ok(/bot\.action\(\/\^final:\(\\d\+\)\$\//.test(SRC), 'هندلرِ دکمه ثبت شده');
  const h = SRC.slice(SRC.indexOf("bot.action(/^final:"));
  ok(/r\.user_id !== uid \|\| r\.status !== 'started'\) return;/.test(h),
    'مالکیتِ رکورد و وضعیت چک می‌شود');
  // قفل باید **قبل از** اولین await باشد وگرنه دوبار-تپ جمع‌بندی را دو بار می‌فرستد
  const iLock = h.indexOf('finalDone: true'), iAwait = h.indexOf('await finishReading');
  ok(iLock > 0 && iLock < iAwait, 'قفلِ دوبار-تپ سینکرون و قبل از await است');
  ok(/s\.finalDone\) return;/.test(h), 'تپِ دوم بی‌اثر است');
}

console.log('\n▶ فشرده‌سازیِ چیدمانِ بزرگ');
{
  // تحقیق ۲: «حتی در چیدمان‌های بزرگ، فالگیران ماهر ساختار را فشرده می‌کنند تا از
  // سردرگمیِ ذهنیِ مشتری جلوگیری شود». فالِ ۱۰کارتی در تست دیوارِ متن شده بود.
  ok(/spread\.size >= 6 \? '\*\*فقط یک جمله‌ی کوتاه\*\*'/.test(LOC),
    'در چیدمانِ ۶ کارت به بالا، هر کارت فقط یک جمله می‌گیرد');
  ok(/حرفی که در معرفیِ کارت زدی را تکرار نکن/.test(LOC),
    'خوانش، تیزر را تکرار نمی‌کند (ضدِ افزونگی)');
}

console.log('\n▶ ایموجیِ بخش‌بندی (از کد، نه از مدل)');
{
  ok(/export const SECT = \{/.test(CORE), 'نشانه‌های بخش در یک نقطه‌ی کد تعریف شده‌اند');
  ok(/بدونِ ایموجی/.test(V4), 'مدل همچنان اجازه‌ی ایموجی ندارد (وگرنه سلیقه‌ای می‌پاشد)');
  for (const k of ['headline', 'pattern', 'card', 'closing']) {
    ok(new RegExp(`SECT\\.${k}`).test(CORE), `بخشِ ${k} نشانه دارد`);
  }
}

console.log('\n▶ بازخوردِ ۱ تا ۵ در آخرِ کار');
{
  ok(/rateAsk:/.test(LOC) && /از ۱ تا ۵/.test(LOC), 'سؤالِ بازخورد مقیاسِ ۱ تا ۵ دارد');
  ok(/راضی بودی/.test(LOC), 'سؤال **رضایت** را می‌سنجد، نه انطباق با واقعیت');
  ok(/fbr:\$\{n\}:\$\{readingId\}/.test(SRC), 'دکمه‌ها readingId را حمل می‌کنند (بدونِ وابستگی به state)');
  ok(/bot\.action\(\/\^fbr:\(\[1-5\]\):\(\\d\+\)\$\//.test(SRC), 'هندلرِ بازخوردِ عددی ثبت شده');
  ok(/if \(r\.feedback\) return;/.test(SRC), 'ثبتِ یک‌باره (دوبار-تپ دو رویداد نمی‌سازد)');
  ok(/r\.user_id !== uid\) return;/.test(SRC.slice(SRC.indexOf("bot.action(/^fbr:"))),
    'مالکیتِ رکورد چک می‌شود');
  ok(/`rate:\$\{score\}`/.test(SRC),
    'مقدار با پیشوندِ rate: ذخیره می‌شود تا از yes/some/no قدیمی جدا بماند (بند ۲ج/۳)');
  ok(/EVENTS\.FEEDBACK, \{ reading_id: readingId, score, scale: 5 \}/.test(SRC),
    'propهای جدید اضافه شده‌اند، propهای قدیمی عوض نشده‌اند');
}

console.log('\n▶ درسِ اولین اجرای آزمایشگاه (۱۴۰۵/۰۵/۲۶)');
{
  // باگِ واقعی: در ۲ فال از ۹، `reads` طولِ درست داشت ولی متنِ کارت‌ها خالی بود، پس
  // رندر همه را دور می‌ریخت و کلِ بلوکِ کارت‌به‌کارت از خوانش غایب می‌شد — یکی‌شان
  // صلیب سلتیِ ۱۰۰٬۰۰۰ تومانی. هیچ خطایی هم لاگ نمی‌شد. تستِ دستی این را نمی‌گیرد
  // چون **نبودنِ** یک بخش را باید بفهمی، نه بودنِ چیزِ غلط.
  ok(/obj\.reads\.slice\(0, cardCount\)\.every\(r => readText\(r\)\.length > 0\)/.test(CORE),
    'خوانشِ خالی خروجیِ نامعتبر است و retry می‌گیرد، نه اینکه بی‌صدا حذف شود');
  ok(core.checkV4Shape({ cards: [{ teaser: 't' }, { teaser: 't' }], reads: [{ text: '' }, { text: '' }], closing: 'c' }, 2) === false,
    'منطقش واقعاً اجرا می‌شود: reads خالی رد');
  ok(core.checkV4Shape({ cards: [{ teaser: 't' }, { teaser: 't' }], reads: [{ text: 'a' }, { text: 'b' }], closing: 'c' }, 2) === true,
    'خروجیِ سالم همچنان پذیرفته می‌شود');
  // مدل گاهی به‌جای [{text}] آرایه‌ی رشته می‌دهد؛ محتوایش نباید دور ریخته شود
  ok(core.checkV4Shape({ cards: [{ teaser: 't' }, { teaser: 't' }], reads: ['الف', 'ب'], closing: 'c' }, 2) === true,
    'آرایه‌ی رشته هم شکلِ معتبری است (ضدِ دورریزِ بی‌صدا)');
  ok(core.renderV4({ reads: ['الف', 'ب'], pattern: 'p' }, [1, 2], ['کارت اول', 'کارت دوم']).body.includes('کارت اول الف'),
    'رندر هم هر دو شکل را می‌سازد');

  // نشتِ few-shot، بارِ سوم از همان خانواده: مثالِ سرخط در ۴ فال از ۹ عیناً کپی شد
  // («بله با احتمالِ بالا…»). قاعده همان است: **هیچ فیلدی نمونه‌ی قابلِ کپی ندارد.**
  ok(!/مثل: «بله با احتمال/.test(V4), 'مثالِ قابلِ کپیِ سرخط حذف شد');
  ok(/یک عبارتِ ثابت برای همه‌ی فال‌ها ممنوع است/.test(V4), 'قیدِ احتمال باید هر بار از نو انتخاب شود');

  // زمانِ ساختگی از فیلدِ callback به بقیه‌ی متن سرریز کرد. راه‌حلِ نهایی وصله‌ی
  // بیشتر نبود، حذفِ خودِ داده بود (بخشِ «زمانِ گذشته» پایین‌تر).
  ok(!/چه‌وقت/.test(V4), 'هیچ ارجاعی به فیلدِ زمانی در پرامپت نمانده');
}

console.log('\n▶ قرارداد فیلدهای اجباری و اختیاری (بودجه‌ی retry)');
{
  // بندِ ۹/۰ ریشه: retry فقط خرجِ چیزی می‌شود که کاربر از نبودش ناراضی می‌شود.
  // ریاضیِ واقعیِ صلیب سلتی: هر retry حدود ۰.۶۷ سنت، و دو-ریکوئستی‌کردن +۲۳٪ چون
  // پرامپتِ سیستم دو بار پول می‌خورد. پس ارزان‌ترین کار این است که اصلاً برای
  // فیلدِ غیرحیاتی retry نکنیم.
  ok(/export function softMissesV4/.test(CORE), 'فیلدهای اختیاری یک تابعِ مشخص دارند');
  ok(/softMissesV4\(parsed\)/.test(SRC), 'جاماندنِ فیلدِ اختیاری لاگ می‌شود (اختیاری ≠ بی‌اهمیت)');

  const hard = { cards: [{ teaser: 't' }], reads: [{ text: 'r' }], closing: 'c' };
  ok(core.checkV4Shape(hard, 1) === true, 'سه فیلدِ اجباری کافی‌اند (pattern لازم نیست)');
  ok(core.checkV4Shape({ ...hard, cards: [{ teaser: '' }] }, 1) === false, 'تیزرِ خالی رد می‌شود (افشا بدونش می‌شکند)');
  ok(core.checkV4Shape({ ...hard, closing: '' }, 1) === false, 'جمع‌بندیِ خالی رد می‌شود');
  ok(core.checkV4Shape({ ...hard, reads: [{ text: '' }] }, 1) === false, 'خوانشِ خالی رد می‌شود');
  ok(JSON.stringify(core.softMissesV4({ pattern: 'p', summary: 's', memory: 'm' })) === '[]',
    'خروجیِ کامل هیچ فیلدِ جامانده‌ای ندارد');
  ok(core.softMissesV4({ pattern: '' }).includes('pattern'), 'نبودِ الگو شمرده می‌شود ولی retry نمی‌گیرد');
}

console.log('\n▶ اندازه‌ی پرامپت');
{
  // پرامپتِ متورم روی Flash هم گران است هم کیفیت را پایین می‌آورد (درسِ v3.1.0).
  // v4 عمداً بزرگ‌تر از v2 است چون سیستمِ کاملی را حمل می‌کند، ولی همچنان سقف دارد.
  ok(V4.length < 6200, `پرامپتِ v4 زیر سقف است (${V4.length} کاراکترِ سورس، سقف ۶۲۰۰)`);
}

console.log('\n▶ درس‌های دورِ سوم (۱۴۰۵/۰۵/۲۶ عصر)');
{
  // باگِ «پارسال»: مدل به فالی که ۱۰ دقیقه قبل بود گفت «پارسال». علتش توهمِ محض نبود —
  // رکوردهای قبلی **هیچ تاریخی** نداشتند و مدل فقط `today` را می‌دید، پس زمان را از
  // خودش ساخت. حل با **داده**، نه با دستور.
  // ⏱ زمانِ گذشته کاملاً از سیستم حذف شد (تصمیمِ مالک، ۱۴۰۵/۰۵/۲۶). تاریخچه‌اش درسِ
  // «چشمش را درست نکن، گوشش را کر نکن» است: اول مدل زمان را از خودش ساخت («پارسال»
  // برای فالِ ۱۰ دقیقه پیش)، پس داده‌ی دقیق دادیم؛ بعد مدل داده را هم نادیده گرفت
  // («هفته‌های قبل»)، پس قاعده‌ی پرامپت دادیم؛ بعد قاعده را سراسری کردیم. سه لایه وصله
  // روی چیزی که اصلاً لازم نبود. کاربر در بخشِ یادآوری نمی‌خواهد بداند فالِ قبلی کِی
  // بوده، می‌خواهد بداند یادت هست چه پرسیده. پس **خودِ داده** حذف شد.
  ok(!/چه‌وقت/.test(CORE), 'هیچ فیلدِ زمانی در کانتکستِ فال‌های قبلی نیست');
  ok(!/export function agoFa/.test(CORE), 'محاسبه‌ی فاصله‌ی زمانی حذف شد (کدِ مرده نماند)');
  ok(core.agoFa === undefined, 'agoFa دیگر صادر نمی‌شود');
  ok(/\*\*به گذشته هیچ اشاره‌ی زمانی نکن\.\*\*/.test(V4), 'پرامپت اشاره‌ی زمانیِ گذشته را ممنوع می‌کند');
  ok(/بازه‌ی زمانیِ \*\*آینده\*\* در جمع‌بندی برعکس، لازم است/.test(V4),
    'بازه‌ی زمانیِ آینده عمداً استثناست (کاربر می‌خواهد بداند کِی)');
  ok(/بازه‌ی زمانیِ تقریبی بده/.test(V4), 'جمع‌بندی همچنان بازه‌ی آینده می‌دهد');

  // تکنیک‌های تحقیق ۲ که **پذیرفته** شدند (به‌شکلِ لنگرخورده، نه چاپلوسانه)
  ok(/یک توانِ استفاده‌نشده‌ی او را نام ببر/.test(V4), 'گزاره‌ی ژاک: توانِ دیده‌نشده، لنگرخورده به کارت');
  ok(/هر دو سو را در یک نفس بگو/.test(V4), 'تله‌ی رنگین‌کمان به‌شکلِ صادقانه (با جهتِ نهایی)');
  // و تکنیکی که **رد** شد، با دلیلِ خودِ تحقیق ۱
  // شات‌گانینگ قبلاً با یک جمله‌ی توصیفی ممنوع شده بود («ادعای زیاد نپاشان») و دورِ
  // چهارم نشان داد اثر ندارد: ۲۴٪ جمله‌ها هیچ لنگری نداشتند و در یکی از فال‌ها ۵۵٪.
  // جایش یک **رویه‌ی قابلِ اجرا** نشست: سؤالِ لنگر قبل از هر جمله، با تکلیفِ روشنِ
  // «حذف یا بازنویسی». همان چیزی که `anchorScore` آزمایشگاه مکانیکی می‌سنجد.
  ok(/لنگرِ این جمله کدام است/.test(V4), 'ضدِ شات‌گانینگ به‌صورتِ سؤالِ قابلِ اجرا، نه توصیه');
  ok(/یا حذفش کن یا با لنگر بازنویسی‌اش کن/.test(V4), 'تکلیفِ جمله‌ی بی‌لنگر روشن است');

  // سؤالِ بازخورد باید همان چیزی را بپرسد که در آنبوردینگ ادعا می‌کنیم
  ok(/نزدیک بودنِ انرژیِ جوابِ فال به حس و نیتی که توی سؤالت داشتی/.test(LOC),
    'سؤالِ بازخورد با ادعای ۸۶٪ هم‌راستا شد');
  ok(/۵ = بیشترین رضایت/.test(LOC), 'معنیِ دو سرِ مقیاس توضیح داده شده');
  // ادعا از دیتا جلو نزند (بند ۱۰ ریشه)
  ok(!/۸۶٪[^\n]*کاملا/.test(LOC), 'ادعای ۸۶٪ کلمه‌ی «کاملاً» را ندارد (عددش ۳۷٪ است)');
}

console.log('\n▶ سقفِ ورودیِ مدل (Gemini Flash: ورودیِ متورم = خروجیِ ضعیف)');
{
  // همه‌ی اجزای کانتکست باید سقفِ عددی داشته باشند، وگرنه ورودی با گذشتِ زمان
  // بی‌صدا باد می‌کند: حافظه‌ی یک کاربرِ قدیمی، یا لیستِ فال‌های قبلی‌اش.
  ok(/setMemory\.run\(llm\.memory\.trim\(\)\.slice\(0, 1200\)/.test(SRC), 'حافظه سقفِ ۱۲۰۰ کاراکتری دارد');
  ok(/String\(parsed\.summary \|\| ''\)\.slice\(0, 300\)/.test(SRC), 'خلاصه‌ی هر فال سقفِ ۳۰۰ کاراکتری دارد');
  ok(/question: question\.slice\(0, 1500\)/.test(SRC), 'سؤال سقفِ ۱۵۰۰ کاراکتری دارد');
  ok(/lastDelivered\.all\(user\.telegram_id, 4\)/.test(SRC), 'فقط ۴ فالِ قبلی به کانتکست می‌رود');

  // و مهم‌تر: **جمعِ** بدترین حالت باید زیرِ بودجه بماند. این ادعا همان چیزی است که
  // اگر فردا کسی سقفِ حافظه را ۵۰۰۰ کند یا ۲۰ فالِ قبلی بفرستد، قرمز می‌شود.
  const MEM = 1200, Q = 1500, PREV = 4 * (300 + 80), KB_PER_CARD = 320, MAX_CARDS = 10;
  const sysMax = 6200; // سقفِ سورسِ پرامپت (پایین‌تر جداگانه ادعا شده)
  const worst = sysMax + MEM + Q + PREV + KB_PER_CARD * MAX_CARDS + 1200 /* بقیه‌ی فیلدها */;
  ok(worst <= 15000, `بدترین حالتِ ورودی زیرِ سقف است (${worst} کاراکتر ≈ ${Math.round(worst / 2.2)} توکن)`);
}

console.log(`\n${errs.length ? '❌' : '✅'} نتیجه: ${pass} پاس، ${errs.length} خطا`);
if (errs.length) { errs.forEach(e => console.log(`   - ${e}`)); process.exit(1); }
