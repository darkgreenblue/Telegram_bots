# توکن ورود به داشبورد — یک رشته‌ی تصادفی بلند (حداقل ۳۲ کاراکتر) بساز و همین را در
# GitHub Secrets با نام DASHBOARD_TOKEN هم ثبت کن (دیپلوی همین فایل را از Secrets می‌سازد).
DASHBOARD_TOKEN=

# پورت لوکال (پیش‌فرض 8787) — سرور فقط روی 127.0.0.1 گوش می‌دهد و از طریق Cloudflare Tunnel در دسترس است
PORT=8787
