# انتخابِ مدل per زبان — تحقیق و اعدادِ راستی‌آزمایی‌شده

> سند تصمیم برای بندِ ۲و (چندزبانگیِ تاروت). دو بخش دارد: **اعدادِ قطعی** (مستقیم از خودِ
> OpenRouter) و **شواهدِ کیفی** (کامیونیتیِ روسی‌زبان). هر ادعا برچسبِ منبع دارد؛ چیزی که
> تأیید نشده صریح «تأییدنشده» علامت خورده است.
> تاریخِ اعداد: **۱۴۰۵/۰۶/۰۹ (۳۱ اوت ۲۰۲۶)**. با `Reading lab → list_models` دوباره بگیر.

## ⏰ فوری و مستقل از چندزبانگی: مدلِ رباتِ زنده تاریخِ انقضا دارد

`google/gemini-2.5-flash` — مدلِ **فعلیِ تاروتِ فارسی و voice2text** — تاریخِ خاموشیِ
اعلام‌شده‌ی **۱۶ اکتبر ۲۰۲۶** دارد، یعنی حدودِ شش هفته بعد از این سند.

⚠️ **تصحیحِ ۱۴۰۵/۰۶/۰۹ (مالک درست شک کرد و بازبینی شد):** نسخه‌ی اولِ همین بند این
تاریخ را قطعی جا زده بود. دو چیز باید کنارش بیاید:
- **گوگل این تاریخ‌ها را «زودترین تاریخِ ممکن» منتشر می‌کند، نه ددلاینِ قطعی.** تاریخِ
  واقعی بعداً تثبیت و از قبل اعلام می‌شود. پس «۱۶ اکتبر» سقفِ برنامه‌ریزی است نه
  روزِ قطعیِ خاموشی.
- 🔒 **و من نتوانستم منبعِ رسمی را باز کنم:** پراکسیِ این محیط `ai.google.dev` و
  `benchr.org` را می‌بندد. این ادعا از **خلاصه‌ی نتایجِ جستجو و منابعِ ثانویه** آمده،
  نه از خواندنِ خودِ صفحه‌ی deprecationهای گوگل. قبل از هر تصمیمِ پرهزینه، مالک یک بار
  خودش `ai.google.dev/gemini-api/docs/deprecations` را باز کند.

یک تاپیکِ انجمنِ توسعه‌دهندگانِ گوگل هم با عنوانِ «Gemini 2.5 Flash deprecated without
warning earlier than shutdown date» وجود دارد که با گزارشِ ۴۰۴ زودهنگام می‌خواند، ولی
خودِ تاپیک هم از پشتِ همین پراکسی **خوانده نشد**، پس محتوایش تأییدنشده است.

**این یک ریسکِ رباتِ زنده است، نه یک کارِ چندزبانگی.** اگر مدل خاموش شود،
`orChatResilient` سه بار تلاش می‌کند، بعد به فالبک می‌افتد؛ فالبک متن‌محور است و
**صدا نمی‌فهمد**، پس مسیرِ ویس کاملاً می‌شکند و بقیه‌ی فال‌ها روی کیفیتِ فالبک می‌افتند.

⚠️ **«جانشینِ رسمی `gemini-3.5-flash` است» درست است، ولی برای ما مهاجرتِ هم‌رده نیست.**
منابع `gemini-3.5-flash` و `gemini-3.6-flash` را جانشینِ پیشنهادی می‌گویند و این ادعای
غلطی نیست؛ حرفِ ما فقط درباره‌ی **قیمت** است: با عددِ واقعیِ
OpenRouter آن مدل **$۰.۰۱۸ per فال** است، یعنی **~۳.۹ برابرِ** مدلِ فعلی. آن مهاجرت
نیست، یک ردیفِ قیمتیِ دیگر است. جانشین‌های واقعیِ هم‌رده در جدولِ زیر.

## اعدادِ قطعی (منبع: `openrouter.ai/api/v1/models`، با `tools/or-models.mjs`)

هزینه برای یک فالِ متوسط: **۳۰۰۰ توکن ورودی + ۱۵۰۰ خروجی**.

| slug | $/M ورودی | $/M خروجی | $/فال | نسبت به فعلی | صدا؟ |
|---|---|---|---|---|---|
| `google/gemini-2.5-flash` **(فعلی)** | 0.300 | 2.500 | **0.00465** | — | ✅ |
| `deepseek/deepseek-v3.2` **(فالبکِ فعلی)** | 0.269 | 0.400 | 0.00141 | −70٪ | ❌ |
| `qwen/qwen3.8-flash` | 0.150 | 0.470 | **0.00115** | **−75٪** | ❌ (تصویر/ویدیو) |
| `qwen/qwen3.7-plus` | 0.320 | 1.280 | **0.00288** | **−38٪** | ❌ (تصویر) |
| `google/gemini-3.1-flash-lite` | 0.250 | 1.500 | **0.00300** | **−35٪** | ✅ |
| `qwen/qwen3.5-plus-02-15` | 0.260 | 1.560 | 0.00312 | −33٪ | ❌ |
| `openai/gpt-5-mini` | 0.250 | 2.000 | **0.00375** | **−19٪** | ❌ |
| `qwen/qwen3.6-plus` | 0.325 | 1.950 | 0.00390 | −16٪ | ❌ |
| `google/gemini-3.6-flash` | 0.750 | 3.750 | 0.00788 | +69٪ | ✅ |
| `google/gemini-3.7-flash` | 0.750 | 3.750 | 0.00788 | +69٪ | ✅ |
| `openai/gpt-5.4-mini` | 0.750 | 4.500 | 0.00900 | +94٪ | ❌ |
| `anthropic/claude-haiku-4.5` | 1.000 | 5.000 | 0.01050 | +126٪ | ❌ |
| `google/gemini-3.5-flash` | 1.500 | 9.000 | 0.01800 | **+287٪** | ✅ |

**دو نکته‌ی ساختاری که از همین جدول درمی‌آید:**

1. **فقط مدل‌های Gemini صدا می‌فهمند.** مسیرِ ویسِ ما از v3.4.0 فایلِ صوتی را مستقیم
   به‌عنوان `input_audio` در **همان یک فراخوانی** می‌فرستد. یعنی هر بازوی غیر-Gemini
   (GPT، Qwen، Claude، DeepSeek) **نمی‌تواند** مسیرِ ویس را بگیرد و به یک مرحله‌ی
   رونویسیِ جدا نیاز دارد — دقیقاً همان چیزی که مالک گفت («فوقش با ویسپر حلش می‌کنیم»).
   گزینه‌ی ارزانِ رونویسی روی همین OpenRouter: `mistralai/voxtral-small-24b-2507`
   (`text+audio`, $۰.۱/$۰.۳ per M). ⚠️ ولی این یعنی برگشت به **دو فراخوانی** per فالِ
   صوتی، که خودِ v3.4.0 عمداً حذفش کرد. پس بازوی غیر-Gemini فقط وقتی می‌ارزد که برتریِ
   کیفیِ متن‌اش این هزینه‌ی ساختاری را جبران کند.
2. **مالیاتِ توکنِ سیریلیک روی این جدول اعمال نشده.** متنِ روسی حدودِ **۲ برابرِ**
   انگلیسی توکن می‌گیرد و ضریبش بینِ سازنده‌ها فرق دارد (تخمینِ منابع: خانواده‌ی
   Gemini و GPT کاراتر، Qwen و DeepSeek ناکاراتر). یعنی برتریِ قیمتیِ Qwen روی کاغذ
   بزرگ‌تر از واقعیت است. **تنها عددِ قابلِ استناد `llm_usage.cost_usd` از یک اجرای
   واقعی است**، نه این جدول.

## شواهدِ کیفی برای روسی (منبع: کامیونیتی و بنچمارک‌های روسی‌زبان)

> ⚠️ **حدِ اعتبار:** این بخش با جست‌وجوی وب جمع شده و **منابع اصلی مستقیم باز نشدند**
> (پروکسیِ محیط `habr.com`، `reddit.com`، `huggingface.co` و بقیه را می‌بندد). نقل‌قول‌ها
> در «محتوا» قابلِ اتکا هستند، در «عینِ عبارت» نه. Reddit اصلاً پوشش داده نشد.

- **بنچمارکی که دقیقاً چیزِ ما را می‌سنجد `RuQualBench` است**: خطا per ۱۰۰۰ توکنِ متنِ
  روسی (جنسِ دستوریِ قاطی، نویسه‌ی الفبای بیگانه، کلمه‌ی ساختگی) روی دو نوع کار که یکی‌شان
  **نوشتنِ داستانیِ بلند** است. متأسفانه ردیفِ هیچ‌کدام از مدل‌های ردهٔ ارزان به دست نیامد.
  چون open source است، **اجرای خودش روی کاندیداها بهتر از ادامه‌ی جست‌وجوست**.
- **MERA و ruMMLU و MMLU-ProX استدلال می‌سنجند، نه نثر.** نمره‌ی بالای MERA هیچ نمی‌گوید
  که مدل مثل یک راویِ گرمِ روسی حرف می‌زند یا نه. برای این محصول تقریباً بی‌ربط‌اند.
- **مدل‌های بومیِ روسی (GigaChat، YandexGPT) صدرِ کیفیتِ زبان‌اند ولی روی OpenRouter
  نیستند.** یعنی سقفِ ما ساختاراً پایین‌تر از یک API روسی است. تصمیم: سراغشان نمی‌رویم.
- **Qwen غافلگیریِ مثبت، DeepSeek غافلگیریِ منفی.** چند منبعِ مستقلِ روسی می‌گویند Qwen
  با اینکه چینی است، روسیِ **طبیعی‌تری** از DeepSeek می‌نویسد؛ و نویسنده‌ی RuQualBench
  می‌گوید `V3.2` نسبت به `V3` **پس‌رفت** کرده — یعنی دقیقاً فالبکِ فعلیِ ما.
- **🔴 خطرِ شماره‌یکِ مدل‌های چینی روی روسی: نویسه‌ی CJK وسطِ متن.** در یک مطالعه‌ی تولیدی
  **۷ مدل از ۱۸** هیروگلیف داخلِ متنِ روسی گذاشتند، و یک موردِ نام‌بُرده‌ی تولیدی با
  `deepseek-v4-flash` هست. **گاردش قبلاً ساخته شد** (`lang/ru.mjs` → `alien`)، پس هر
  دورِ آزمایشگاه این را خودکار می‌گیرد.
- **«—» در روسی هم امضای متنِ ماشینی است.** بحث‌های روسی صریحاً «نقطه‌گذاریِ بیش‌ازحد
  درست» را نشانه‌ی متنِ هوش مصنوعی می‌دانند. یعنی قاعده‌ی بند ۱۰ ریشه در روسی **حتی
  مهم‌تر** است، و چون منبعش RLHF است با پرامپت به‌تنهایی پاک نمی‌شود. گاردِ ما از قبل
  در `noDash` و در سنجه‌ی آزمایشگاه هست.
- **ضعفِ شناخته‌شده‌ی GPT روی روسی: «انگلیسیزم»** (کلمه‌ی انگلیسی‌تبار، گاهی ساختگی،
  داخلِ متنِ روسی) و **لحنِ سردتر**. هر دو دقیقاً همان چیزی‌اند که این محصول نمی‌خواهد.
  گاردِ «واژه‌ی لاتین» در `lang/ru.mjs` مخصوصِ همین است.
- **⚠️ تأییدنشده:** رفتارِ مدل‌ها روی لغزشِ «ты»→«вы» وسطِ تولید، و اینکه کدام مدل
  روی محتوای «گадание» دیسکلیمر می‌چسباند. هیچ شواهدِ per مدلی پیدا نشد. هر دو در
  سنجه‌های آزمایشگاه هست، پس با دیتای خودمان جواب می‌گیرند نه با جست‌وجو.

## برنامه‌ی بازوها (روسی)

| دور | مدل | چرا |
|---|---|---|
| ۱ (خطِ پایه) | `google/gemini-2.5-flash` | همان مدلِ محصول. تا نفهمیم روسی روی **مدلِ فعلی** چه نمره‌ای می‌گیرد، هیچ مقایسه‌ای معنی ندارد |
| ۲ | `google/gemini-3.1-flash-lite` | **۳۵٪ ارزان‌تر، و صدا می‌فهمد.** طبیعی‌ترین جانشینِ مدلِ در حالِ بازنشستگی |
| ۳ | `openai/gpt-5-mini` | خواسته‌ی صریحِ مالک (یک مدلِ GPT هم‌قیمت). ۱۹٪ ارزان‌تر |
| ۴ | `qwen/qwen3.7-plus` | تنها مدلِ ارزانی که شواهدِ **مستقیمِ** کامیونیتی برای «روسیِ طبیعی» دارد |

نتیجه‌ی هر دور با همان سنجه‌های مکانیکی سنجیده می‌شود و **هزینه‌ی واقعیِ روسی** از خودِ
اجرا درمی‌آید، نه از جدولِ بالا.

---

## ✅ نتیجه‌ی اجرا (ده دور تمام شد) — برنده: `google/gemini-3.1-flash-lite`

برنامه‌ی بالا اجرا شد. جزئیاتِ دور-به-دور در `bots/tarot/CLAUDE.md`؛ این‌جا فقط حکم و
اعدادِ **واقعی**:

| مدل | بی‌لنگر | فالِ ایرادناک | تأخیرِ تعمیر | صدا می‌فهمد؟ |
|---|---|---|---|---|
| **`gemini-3.1-flash-lite`** ✅ | **۲۷ تا ۳۰٪** | **۱ از ۲۷** | ۸۶۸ تا ۱۵۷۱ms | ✅ |
| `gemini-2.5-flash` (فعلی) | ۲۹٪ | ۳ از ۹ | ۹۲۶ تا ۱۳۹۹ms | ✅ |
| `deepseek-v3.2` | ۳۸٪ | ۱ از ۹ | تا **۳۱۶۰۰ms** | ❌ |
| `openai/gpt-5-mini` | ۴۰٪ | ۶ از ۹ ⚠️ | مسیرِ تعمیر مرده | ❌ |

- **هزینه‌ی واقعیِ برنده: $۰.۰۰۲۵۰ per فال** (۲۷ فال، از `usage.cost` خودِ OpenRouter).
- ⚠️ **تصحیحِ یک عددِ همین سند:** پیش‌تر ثبت شده بود روسی ~۴۰٪ گران‌تر از فارسی است. آن
  رقم از گزارشی می‌آمد که دلار را با **قیمتِ هاردکدِ Gemini Flash** حساب می‌کرد. با عددِ
  واقعی، روسی روی این مدل **$۰.۰۰۲۵** است در برابرِ ~$۰.۰۰۳۶ فارسی روی مدلِ فعلی، یعنی
  **ارزان‌تر**. مالیاتِ توکنِ سیریلیک واقعی است (۲.۱۷ توکن per کلمه، اندازه‌گیریِ مستقل)،
  ولی ارزان‌تر بودنِ مدلِ تازه از آن بیشتر است.
- **`qwen3.7-plus` اجرا نشد** و دلیلش صادقانه ثبت می‌شود: بودجه‌ی ده دور صرفِ دو بازویی
  شد که مالک صریحاً خواسته بود (GPT و DeepSeek) به‌علاوه‌ی سه دوری که فیکسِ باگ‌های
  خودِ ابزار لازم داشت. شواهدِ بیرونی هم علیه خانواده‌ی چینی بود (نویسه‌ی غیرسیریلیک در
  متنِ روسی، `I18N-RU-LANGUAGE-RESEARCH.md`).
- **این حکم برای رباتِ فارسیِ زنده هم هست:** مدلِ فعلی اکتبر ۲۰۲۶ بازنشسته می‌شود و حالا
  یک جانشینِ سنجیده‌شده و صداشنو داریم. مهاجرتِ فارسی یک PR جداست (بند ۲ج/۴: تغییرِ
  رفتاری، پس bump نسخه و ترجیحاً A/B).

⚠️ **قاعده‌ی روش (درسِ دورهای ۴ تا ۱۶ فارسی):** یک اجرای ۹ فالی نویزِ ±۶ واحد دارد. هر
تفاوتی که از این کوچک‌تر باشد بی‌معناست و باید با `reps` چندپاسه سنجیده شود. مقایسه‌ی
مدل‌ها فقط وقتی معتبر است که **همه‌ی بازوها همان سناریو و همان seed** را بگیرند؛ که
می‌گیرند، چون seed از `lab:<persona>:<i>` می‌آید و به مدل ربطی ندارد.


## 🔴 حکمِ فارسی: `gemini-3.5-flash-lite` جانشینِ مدلِ زنده **نمی‌شود**

مالک خواست این مدل کنارِ بقیه تست شود، و چون هم‌قیمتِ مدلِ زنده است و مدلِ زنده تاریخِ
انقضا دارد، نامزدِ جدی‌ای بود. نتیجه‌ی آزمایشگاه روشن و منفی است.

**مقایسه‌ی جفت‌شده (یک اجرا، عینِ همان کارت‌ها و سؤال‌ها، ۲ پاس، ۳۶ فال):**

| سناریو | `gemini-2.5-flash` | `gemini-3.5-flash-lite` | تفاضل |
|---|---:|---:|---:|
| P1.3 | ۷٪ | ۲۴٪ | +۱۷ |
| P2.1 | ۱۳٪ | ۴۵٪ | +۳۳ |
| P2.2 | ۴۳٪ | ۵۰٪ | +۷ |
| P2.3 | ۱۲٪ | ۳۴٪ | +۲۳ |
| P3.1 | ۲۹٪ | ۲۱٪ | **−۸** |
| P3.2 | ۱۱٪ | ۳۲٪ | +۲۱ |
| P3.3 | ۲۷٪ | ۳۰٪ | +۴ |

**میانگینِ تفاضل per سناریو: +۱۲٫۲ واحد بدتر. بردِ سناریویی: ۱ بهتر، ۸ بدتر، ۰ مساوی.**
الگو یک‌دست است، پس این تفاوت قابلِ اتکاست، برخلافِ شکافِ فارسی/روسی که نبود.

**و یک ایرادِ کیفیِ مستقل:** در اجرای تک‌بازوییِ ۲۷ فالی، این مدل **۶ فالِ ایرادناک**
داد در برابرِ **۱** فالِ مدلِ فعلی، و هر شش تا **همان یک ایراد** بودند: «پارسال» یا
«سالِ پیش». یعنی مدل گذشته‌ای را به کاربر نسبت می‌دهد که **اصلاً داده‌اش را نداریم**.
این دقیقاً همان باگی است که بند ۹/۰ ریشه سه لایه وصله خورد تا آخر معلوم شد باید خودِ
فیلد حذف شود. مدلی که این را بازتولید می‌کند نباید روی رباتِ درآمدزا برود.

**و گران‌تر هم هست، با اینکه نرخش برابر است:** نرخِ هر دو ۰٫۳۰۰/۲٫۵۰۰ است، ولی
هزینه‌ی واقعیِ per فال $۰٫۰۰۴۶۵ در برابرِ $۰٫۰۰۳۷۲ شد، چون این مدل **بلندتر می‌نویسد**
(۴۹۶ جمله در برابرِ ۴۱۵ در همان تعداد فال). نرخِ برابر با هزینه‌ی برابر یکی نیست.

### 📏 و مهم‌ترین درسِ روشی: درصدِ تجمیعی این اثر را کاملاً پنهان کرده بود

دو اجرای **جدا** همین دو مدل را ۲۷٪ در برابرِ ۲۸٪ نشان دادند، یعنی مساوی. مقایسه‌ی
جفت‌شده روی همان مدل‌ها ۱۲٫۲ واحد اختلاف و ۸ بردِ سناریویی از ۹ داد.

علتِ ریاضی ساده است: درصدِ تجمیعی **وزنِ جمله‌ای** است، و مدلی که بیشتر می‌نویسد
مخرجِ خودش را بزرگ می‌کند و نرخِ بی‌لنگرِ خودش را رقیق می‌کند. میانگینِ per سناریو
**وزنِ سناریویی** است و این رقیق‌شدگی را ندارد.

پس قاعده‌ی صفرِ آزمایش (ورک‌فلوی زبان) فقط درباره‌ی نویز نیست: **درصدِ تجمیعی حتی
بدونِ نویز هم می‌تواند اثرِ بزرگ و یک‌دست را نامرئی کند.** تصمیمِ مدل باید روی تفاضلِ
جفت‌شده‌ی per سناریو گرفته شود، نه روی یک درصدِ کلی.
