# CODEX.md — راهنمای Codex و همکاری چندایجنتی

## شروع هر سشن

1. `CLAUDE.md` ریشه را کامل بخوان؛ این فایل قرارداد فنی/محصولی اصلی ریپو است.
2. اگر کار روی یک بات است، `bots/<name>/CLAUDE.md` همان بات را هم کامل بخوان.
3. `docs/AI-COLLABORATION.md` را بخوان تا بدانی Claude Code یا Codex چه کاری را شروع کرده، چه چیزی تحویل داده، و کجا باید ادامه دهی.
4. پیش از تغییر، `git status` و `git pull --ff-only` را اجرا کن. اگر تغییر uncommitted یا conflict از عامل دیگری دیدی، آن را بازنویسی نکن؛ ابتدا وضعیت را در لاگ همکاری ثبت و با مالک هماهنگ کن.

## قرارداد همکاری

- `CLAUDE.md` و راهنمای اختصاصی هر بات، منبع حقیقت محصول و عملیات‌اند. `CODEX.md` جایگزین یا خلاصه‌ی آن‌ها نیست.
- هر تغییر باید روی یک branch مستقل انجام شود. branch و commit باید هدف روشن داشته باشند.
- پیش از push، تغییرات، تست‌های مرتبط، ریسک‌ها و راه rollback را در `docs/AI-COLLABORATION.md` ثبت کن.
- در هر PR که رفتار یک بات را تغییر می‌دهد، همان PR باید `bots/<name>/CLAUDE.md` را نیز به‌روز کند؛ برای تغییرات shared/workflow/زیرساخت، `CLAUDE.md` ریشه هم به‌روز می‌شود.
- هیچ secret، فایل `.env`، توکن، داده‌ی واقعی کاربر، یا خروجی حساس را commit نکن.
- merge یا deploy را فقط پس از بررسی CI و قواعد انتشارِ `CLAUDE.md` انجام بده. برای بات‌های زنده، محدودیت‌های migration افزایشی، compatibility، `PRODUCT_VERSION`، rollback و پنجرهٔ امن deploy اجباری‌اند.

## مرزبندی مسئولیت

- هر عامل فقط فایل‌های لازم برای همان تسک را تغییر می‌دهد و از فرمت‌کردن/بازنویسی گسترده‌ی نامرتبط پرهیز می‌کند.
- اگر دو عامل روی فایل یا بات مشترکی کار می‌کنند، عامل دوم ابتدا کارِ اول را از طریق branch/commit/log بررسی می‌کند؛ تغییرِ هم‌پوشان بدون حل‌کردن این تداخل ممنوع است.
- تصمیم‌های محصولی، فرضیه‌ها، تغییرات رفتاری، migration، rollout و rollback باید در مستندات همان بات ثبت شوند، نه فقط در پیام چت.

## تعریف تحویل

یک تغییر زمانی آماده‌ی handoff است که: کد و مستندات همگام‌اند، تست مناسب اجرا شده، خروجی/ریسک/rollback در لاگ همکاری ثبت شده، و working tree تمیز است.
