# Tabir Khab — Project Handoff / سندِ انتقالِ کانتکست

> این فایل برای انتقالِ کاملِ کانتکستِ پروژه به یک سیشنِ جدیدِ Claude Code است.
> در سیشنِ تازه (روی همین ریپو) فقط بگو: **«HANDOFF.md را بخوان»** تا همه‌چیز بازیابی شود.

---

## این پروژه چیست
رباتِ چندزبانه‌ی تعبیر خواب — کاراکترِ **«تعبیرکننده‌ی اعظم»** (Grand Interpreter) در **«قلمرو رویاها»** — روی **بله + تلگرام**. Long-polling، هر دو پلتفرم در یک پروسه. کاربر خواب (متن یا صوت) می‌فرستد؛ ربات یک پیش‌نمایشِ رایگان + تصویر می‌دهد، و تعبیرِ کامل پشتِ «همسفری» (اشتراک) است.

## ریپو و استقرار
- **GitHub:** `darkgreenblue/tabir-khab` (خصوصی)
- **سرور:** ابرِ آروان، Ubuntu 24.04، `ubuntu@185.204.171.170`
  - مسیر: `/home/ubuntu/tabir_khab` · venv: `.venv` (Python 3.12)
  - سرویسِ systemd: `tabir-khab` (enabled، auto-restart)
  - Deploy key فقط‌خواندنی سرور↔گیت‌هاب وصل می‌کند (SSH alias `github-tabir`)
  - ۲GB swap اضافه شده
  - کنارِ رباتِ دیگری به نام `voice2text` (Node/pm2) اجرا می‌شود — **env و کلیدها و توکن‌ها کاملاً جدا، هیچ تداخلی نیست**
- **کلیدِ SSHِ سرور** (روی مکِ کاربر): `~/Downloads/ar-voice2text-bot-privatekey.pem`

## روالِ استقرار (قانونِ جاری)
ویرایش → `commit` → `push` به گیت‌هاب → روی سرور: `cd ~/tabir_khab && git pull && sudo systemctl restart tabir-khab`.
**رباتِ لوکال دیگر اجرا نمی‌شود؛ سورس روی گیت‌هاب/سرور است.**

## قوانینِ جاریِ مهم (همیشه رعایت کن)
1. **هر تغییرِ متنیِ رو‌به‌کاربر روی هر ۶ زبان اعمال می‌شود** (fa, en, ar, ru, es, pt) — بدونِ اینکه هر بار گفته شود.
2. **تفاوت‌های مجاز بین زبان‌ها:** فقط (الف) سؤالِ اولِ آنبوردینگ + مدلِ پرسوناها، و (ب) روشِ پرداخت (فارسی→زرین‌پال/کیف‌پولِ بله، بقیه→Stars/Crypto). باقیِ لحن/برند/تجربه یکسان است.
3. **پرسوناها در تعداد و مدل per-language فرق دارند — هرگز یکی‌شان نکن.** fa چهارتا (religious, traditional, mythological, psychological)؛ en سه‌تا (lucid, therapeutic, newage)؛ ar/ru/es/pt مخصوصِ بازارِ خودشان.
4. **«ری استارت»:** فقط وقتی کاربر صراحتاً بگوید «ری استارت» → DB پاک شود. در غیر این صورت فقط ری‌لودِ کد.
5. فلگ‌های تست در `config.py`: `SKIP_PAYMENT=True`، `SKIP_DAILY_LIMIT=True`، `REFERRAL_ENABLED=False` (رفرال موقتاً خاموش: دکمه‌ی دعوت پنهان، deeplinkِ `ref_` نادیده، پاداش غیرفعال).

## معماری مدل‌ها
- **OpenRouter** (`https://openrouter.ai/api/v1`): همه‌ی LLM. اصلی `google/gemini-2.5-flash` (متن + صوتِ multimodal). فالبک `deepseek/deepseek-v4-pro`. STT فالبک `openai/whisper` (روی OpenRouter ۴۰۰ می‌دهد؛ به‌ندرت لازم می‌شود چون پارسرِ مقاوم داریم).
- **GapGPT** (`https://api.gapgpt.app/v1`): **فقط تصویر**، مدل `gapgpt/z-image`. **هرگز به OpenRouter منتقل نکن.**
- دو کلاینت در `ai.py`: `_gap_client` (تصویر)، `_or_client` (LLM/STT).

## فایل‌ها
- `config.py` — همه‌ی ثابت‌های غیرمحرمانه (مدل‌ها، قیمت‌ها، تایم‌اوت‌ها، فلگ‌ها، قاب‌بندیِ هنریِ تصویر). محرمانه‌ها فقط در `.env`.
- `bot.py` — ورودی، دو polling loop، تنظیمِ لاگ (فایلِ چرخشیِ `logs/bot.log`).
- `bale.py` — رپرِ aiohttp برای API بله+تلگرام.
- `handlers.py` — کلِ منطقِ تعامل (آنبوردینگ، پایپ‌لاینِ خواب، کال‌بک‌ها، پیوال).
- `ai.py` — لایه‌ی LLM/تصویر (`interpret_dream`, `process_voice_dream`, `generate_image`, پارسرها).
- `prompts.py` — سازنده‌ی سیستم‌پرامپت (خروجیِ سه‌فیلدی).
- `profiling.py` — `build_context` از سؤال‌های ۲–۷.
- `db.py` — aiosqlite، هر پلتفرم DB جدا.
- `texts.py` — لایه‌ی متنِ رو‌به‌کاربر روی پکیجِ `locales`.
- `locales/` — `fa, en, ar, ru, es, pt` + `__init__` (با `validate()`؛ fa مرجع است).
- `payments.py` — اشتراک + (رفرال، فعلاً خاموش).
- `deploy/` — یونیتِ systemd + `DEPLOY.md`.

## رفتارها و نکاتِ کلیدی
- **آنبوردینگ:** ۷ سؤال، ادیتِ درجای یک پیام، فقط دکمه‌ی «شروع» و «◀️ پرسش قبل» (نه «بعد»)، هدرِ «🔮 پرسش X از ۷». پاسخ همیشه آخرین انتخاب را overwrite می‌کند.
- **خروجیِ تعبیر = JSON سه‌فیلدی** `{preview, depth, image_prompt}`:
  - `preview` (رایگان): تأییدِ محتوای خواب + قلابِ ریسک/کنجکاوی، بدونِ اشاره به پروفایل، پایان با «…».
  - `depth` (پریمیوم): نشانه‌به‌نشانه با منابعِ پرسونا + ترس/ناخودآگاه/حال/آینده + هم‌نواییِ **زیرپوستیِ** پروفایل (هرگز صریح).
  - `image_prompt`: **فقط توصیفِ بصریِ خالصِ خواب** (همه‌ی نمادها)، بدونِ سبک/سایز.
- **تصویر:** کد `image_prompt` را در `IMAGE_ART_DIRECTION` می‌پیچد → عمودیِ ۹:۱۶ (`1024x1792`، استوری). تنها بررسیِ هاردکد = تصویرِ کاملاً سیاه → یک ریکوئستِ دوم به همان مدلِ اصلی با `IMAGE_SAFE_RETRY_NOTE`. ابعاد و `image_black_retries` در DB ذخیره می‌شوند (هیچ ری‌تلاشی بابتِ ابعاد نیست).
- **پارسرِ مقاومِ JSON** (`_lenient_fields`): JSON خرابِ gemini (کوتیشن/نیولاینِ خام) را نجات می‌دهد. + `_primary_with_format_retry` (تا `PRIMARY_FORMAT_ATTEMPTS=2`).
- **تایم‌اوتِ هر مرحله** (config): `FILE_API_TIMEOUT=30`, `DOWNLOAD_TIMEOUT=120`, `INTERPRET_TIMEOUT=240`, `IMAGE_TIMEOUT=120`. خطا/تایم‌اوت → روایت‌گر کنسل + پیامِ خطای پرسونا + `processing_failed` (قابلِ resume).
- **پردازشِ خواب پس‌زمینه‌ای** (`asyncio.create_task`) تا یک خوابِ کند polling را بلاک نکند. قفلِ `_processing` ضدِ پردازشِ دوباره.
- **Idempotency:** `users.pending_dream_id` ورودی را به تعبیرِ ساخته‌شده پیوند می‌دهد → resume بدونِ فراخوانیِ دوباره‌ی LLM. `dreams.preview` ذخیره می‌شود.
- **مدتِ صوتِ بله به میلی‌ثانیه است** (تلگرام به ثانیه) — در `handlers` با `÷1000` نرمال می‌شود.
- `MAX_VOICE_DURATION=900` ثانیه (۱۵ دقیقه)، هر دو پلتفرم.
- **لاگِ ماندگار:** `logs/bot.log` (چرخشی ۵MB×۱۰). httpx روی WARNING.
- پیوال (آخرین تغییر): متنِ «کلیدِ این دروازه» + دعوتِ همسفری، با اشاره به دو تعرفه‌ی به‌صرفه‌تر؛ **دکمه‌ها (۳ تعرفه با قیمت) دست‌نخورده‌اند** و قیمت فقط روی دکمه است.

## معلق / به‌تعویق‌افتاده
- پرداختِ واقعیِ زرین‌پال (فعلاً stub)
- قیمت‌گذاریِ Telegram Stars / رمزارز
- مدلِ فالبکِ تصویر (هنوز انتخاب نشده)
- فالبکِ whisper روی OpenRouter (۴۰۰ — به‌ندرت لازم)
- پروفایلینگِ پویا per-dream (`profiling.merge_dream_signal` فعلاً no-op)

## ادامه در سیشنِ جدید
۱) `git clone` این ریپو در یک پوشه‌ی تازه (یا روی خودِ سرور).
۲) `claude` را همان‌جا اجرا کن.
۳) بگو: **«HANDOFF.md را بخوان»** → کلِ کانتکست بازیابی می‌شود.
۴) محرمانه‌ها در `.env` هستند (در گیت نیستند) — روی سرور از قبل موجودند؛ برای محیطِ تازه از `.env.example` بساز.
