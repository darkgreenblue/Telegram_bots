# PLATFORM.md — زیرساخت مشترک برای پروژه‌های جدید

> **این فایل را در ریشه‌ی هر ریپوی جدید کپی کن.**
> هر سشن Claude Code که روی آن ریپو کار می‌کند باید **قبل از هر کاری** این فایل را کامل بخواند.
> در `CLAUDE.md` ریپوی جدید، خطِ اول این باشد:
> `> قبل از هر کاری PLATFORM.md را کامل بخوان — زیرساخت مشترک (سرور، کلیدها، دیپلوی، پایش) آنجاست.`

این فایل **قرارداد زیرساخت** است، نه مستنداتِ محصول. یعنی می‌گوید سرور کجاست، کلیدها از کجا
می‌آیند، کد چطور روی سرور می‌رود، خرابی چطور دیده می‌شود، و چه کارهایی روی سرورِ **مشترک**
مطلقاً ممنوع است. اینکه خودِ محصول چه می‌کند، در `CLAUDE.md` همان ریپو نوشته می‌شود.

**مخاطب:** سشنِ Claude Code. مالک غیرفنی است، SSH ندارد، و نباید هیچ دستوری روی سرور بزند.
هر کاری که «باید یک نفر انجام بدهد» یعنی **سشن باید خودش انجامش بدهد** از مسیر کامیت → PR →
merge → دیپلوی خودکار. تنها کارِ دستیِ مجاز برای مالک، ساختنِ Secret در گیت‌هاب و گرفتنِ توکن
از BotFather است.

---

## فهرست

| بند | موضوع |
|-----|--------|
| ۱ | زیرساخت در یک نگاه |
| ۲ | چه چیزهایی از قبل روی سرور هستند (و ممنوعه‌های سرورِ مشترک) |
| ۳ | چک‌لیست راه‌اندازی پروژه‌ی جدید از صفر |
| ۴ | قرارداد کلیدها (GitHub Secrets) |
| ۵ | دیپلوی: فایل آماده + هشت قاعده‌ی آهنین |
| ۶ | CI: فایل آماده |
| ۷ | Ops — دیباگ بدون SSH: فایل آماده |
| ۸ | پایش سلامت: چه چیزی رایگان پوشش دارد، چه چیزی ندارد |
| ۹ | بکاپ دیتابیس: فایل آماده |
| ۹ب | اتصال اختیاری به داشبورد ادمینِ موجود + قرارداد آنالیتیکس |
| ۱۰ | OpenRouter و قواعد هزینه‌ی LLM |
| ۱۱ | ربات تلگرام و بله |
| ۱۲ | پروژه‌های وب: سایت، API، ورکر |
| ۱۳ | پروژه‌های پایتون (الگوی systemd) |
| ۱۴ | سهمیه‌ی GitHub Actions |
| ۱۵ | قواعد مهندسی که با خون نوشته شده‌اند |
| ۱۶ | گردش‌کار توسعه و «تعریفِ تمام‌شدن» |
| ۱۷ | سؤال‌های اجباری قبل از merge و قبل از انتشار |
| ۱۸ | اسکلت `CLAUDE.md` ریپوی جدید |
| ۱۹ | عیب‌یابی سریع |
| ۲۰ | پیشنهادهای اجرانشده |

---

## ۱) زیرساخت در یک نگاه

چهار چیز مشترک است و هیچ‌کدام قرار نیست دوباره ساخته شود:

| قطعه | جزئیات | مشترک یا جدا؟ |
|------|---------|----------------|
| **سرور (VPS)** | `185.204.171.170` · کاربر `ubuntu` · Ubuntu · Node 20 · PM2 · `sudo` بدون رمز | **مشترک** — همه‌ی پروژه‌ها روی همین یک ماشین‌اند |
| **کلید سرور** | GitHub Secret به نام `VPS_SSH_KEY` (کلید خصوصی SSH) | **مشترک** — همان کلید در Secrets هر ریپوی جدید هم گذاشته می‌شود |
| **OpenRouter** | یک حساب، `https://openrouter.ai/api/v1` | حساب مشترک، ولی **کلید per پروژه جدا** (برای اندازه‌گیری مستقل هزینه) |
| **تلگرام / بله** | Bot API | **هر ربات توکن جدا** از BotFather |
| **ادمین/هشدار** | آی‌دی عددی تلگرام مالک در Secret `OWNER_TELEGRAM_ID` | مشترک — همان آدم، ادمینِ همه‌ی محصولات |

> ⚠️ «Node 20» از مستنداتِ مونوریپو می‌آید. اگر پروژه‌ات نسخه‌ی بالاتری لازم دارد، **فرض نکن**:
> اول با `Ops` نسخه‌ی واقعی را ببین. و هرگز نسخه‌ی سراسریِ Node را عوض نکن — `better-sqlite3`
> بیلدِ نیتیو دارد و ربات‌های زنده در اولین ری‌استارت می‌شکنند.

**مسیر رسیدن کد به سرور (تنها مسیر مجاز):**

```
کامیت روی برنچ claude/*  →  PR  →  CI سبز  →  merge به main  →  workflow «Deploy»
   →  SSH به VPS  →  کپی کد  →  ساخت .env از Secrets  →  pm2 reload  →  پیام موفقیت/خطا
```

هیچ‌کس (نه مالک، نه سشن) کد را دستی روی سرور نمی‌گذارد. اگر چیزی روی سرور لازم است، از
مسیر بالا می‌رود.

**مسیر دیدنِ سرور بدون SSH:** ورک‌فلوی `Ops` (بند ۷) که سشن با GitHub MCP اجرا می‌کند و
خروجی را از لاگ جاب می‌خواند.

---

## ۲) چه چیزهایی از قبل روی سرور هستند

این سرور **مشترک** است و روی آن محصولاتِ **زنده و درآمدزا** اجرا می‌شوند. قبل از هر کاری
این جدول را بخوان:

| چیز | مالکش | نکته |
|-----|--------|------|
| `~/voice2text/` | مونوریپوی ربات‌های تلگرام | نامش تاریخی است؛ **کلِ** مونوریپو آنجاست. دست نزن. |
| `~/tabir_khab/` | ربات تعبیر خواب (پایتون + systemd) | دست نزن. |
| اپ‌های pm2: `voice2text`, `tarot`, `tarot-ru`, `tarot-pt`, `tarot-es`, `daily-brief`, `dashboard`, `health-watch` | مونوریپو | **هرگز** stop/restart/delete نکن |
| سرویس systemd: `tabir-khab`, `dash-tunnel`, `pm2-ubuntu` | مونوریپو | `pm2-ubuntu` یعنی بعد از ری‌بوت pm2 خودکار بالا می‌آید |
| پورت `8787` روی `127.0.0.1` | داشبورد ادمین | **اشغال است** — پورت دیگری بردار |
| کرونِ کاربر `ubuntu` سرِ دقیقه‌ی ۳۰ هر ساعت | انتشار روزانه‌ی کانال مارکتینگ | با `crontab -` بازنویسی نکن (بند ۵، قاعده‌ی ۷) |
| `cloudflared` | تونل داشبورد | نصب است؛ برای تونل پروژه‌ی خودت هم قابل استفاده |
| `ffmpeg`, `python3`, `git`, `curl`, `sudo` | سیستم | موجودند (`sudo` بدون رمز برای `ubuntu` کار می‌کند) |

### ⛔ ممنوعه‌های سرورِ مشترک (نقض هرکدام یعنی خواباندنِ محصولِ درآمدزای یک نفر دیگر)

1. **هرگز** `pm2 stop|restart|delete|kill` روی اپی که مالِ پروژه‌ی تو نیست. حتی «برای تست».
2. **هرگز** `pm2 kill` (کلِ دیمِن pm2 و همه‌ی ربات‌ها را می‌کشد).
3. `pm2 save` لیستِ **همه‌ی** اپ‌ها را ماندگار می‌کند. اگر لحظه‌ی `pm2 save` اپِ کسِ دیگری
   `stopped` باشد، همان وضعیت ماندگار می‌شود و ری‌بوت هم نجاتش نمی‌دهد. پس قبل از
   `pm2 save` یک `pm2 ls` بگیر؛ اگر اپی غیر از مالِ خودت `online` نیست، `pm2 save` **نزن**
   و به مالک خبر بده.
4. **هرگز** فایل‌های `~/voice2text/**` یا `~/tabir_khab/**` را نخوان، کپی نکن، یا تغییر نده
   (`.env` های آنجا کلیدهای محصولات زنده‌اند).
5. **هرگز** `crontab -r` و هرگز `crontab -` بدونِ حفظِ خطوطِ موجود (الگوی درست در بند ۵).
6. **هرگز** نسخه‌ی سراسری Node یا `apt upgrade` را بدون تصمیم صریح مالک عوض نکن؛
   `better-sqlite3` بیلدِ نیتیو دارد و با عوض شدن نسخه‌ی Node، ربات‌های زنده در
   لحظه‌ی ری‌استارت می‌شکنند.
7. دیسک **مشترک** است. لاگِ بی‌سقف، بکاپِ محلی و فایلِ موقتِ بزرگ ننویس. ناظرِ سلامت
   بالای ۹۲٪ هشدار می‌دهد ولی تا آن لحظه ممکن است نوشتنِ دیتابیسِ **پول** شکست بخورد.
8. پورتِ عمومی روی اینترنت باز نکن. سرویسِ وب فقط روی `127.0.0.1` گوش می‌دهد و از تونل
   بیرون می‌آید (بند ۱۲).

---

## ۳) چک‌لیست راه‌اندازی پروژه‌ی جدید از صفر

فرض: ریپوی جدید ساخته شده و این فایل داخلش هست. `<PROJ>` را با اسمِ کوتاه و
`kebab-case` پروژه جایگزین کن (مثلاً `shop-bot`). همین اسم می‌شود: نامِ اپِ pm2، نامِ
پوشه روی سرور، و پیشوندِ Secret ها (با حروف بزرگ و آندرلاین: `SHOP_BOT_*`).

**۱. نام‌ها را انتخاب کن و از تصادم مطمئن شو**
- نامِ اپِ pm2 نباید با هیچ‌کدام از اپ‌های جدول بند ۲ یکی باشد. اگر شک داری، اول
  `Ops → status` را روی همین ریپو اجرا کن و لیستِ واقعی را ببین.
- اگر پروژه سرویس وب دارد، پورت را از `8800` به بالا بردار (`8787` اشغال است).

**۲. اسکلت ریپو**
```
<repo>/
  PLATFORM.md              ← همین فایل
  CLAUDE.md                ← مستندات محصول (اسکلت در بند ۱۸)
  ecosystem.config.cjs     ← تعریف اپ‌های pm2 این پروژه
  package.json             ← Node ≥ 20، type: module
  index.js                 ← نقطه‌ی ورود
  .env.example             ← فقط نامِ کلیدها، هرگز مقدار
  .gitignore               ← .env, data/, node_modules/
  .github/workflows/
    deploy.yml  ci.yml  ops.yml  health.yml  backup.yml
  data/                    ← دیتابیس‌ها (در گیت نیست)
```

**۳. Secrets را در ریپوی جدید بساز** (بند ۴). حداقلِ لازم: `VPS_SSH_KEY` و
`OWNER_TELEGRAM_ID`. اگر ربات است: `<PROJ>_BOT_TOKEN`. اگر LLM دارد:
`<PROJ>_OPENROUTER_KEY`.

**۴. ورک‌فلوها را از بندهای ۵ تا ۹ کپی کن** و فقط مقادیرِ بالای هر فایل را عوض کن.

**۵. اولین دیپلوی**: merge به `main` → ورک‌فلوی Deploy خودش پوشه را روی سرور می‌سازد.
هیچ کارِ دستی روی سرور لازم نیست.

**۶. تأیید:** `Ops → status` بزن و ببین اپ `online` است. بعد `Ops → logs` برای دیدن
لاگِ استارت.

**۷. `CLAUDE.md` ریپو را بنویس** (بند ۱۸) و از همان لحظه به‌روز نگهش دار.

---

## ۴) قرارداد کلیدها (GitHub Secrets)

**تک‌منبعِ حقیقتِ همه‌ی کلیدها = GitHub Secrets همان ریپو**
(`Settings → Secrets and variables → Actions`). هیچ کلیدی در کد، در کامیت، در چت، یا
دستی روی سرور نمی‌نشیند. دیپلوی فایل `.env` را روی سرور **از روی Secrets می‌سازد**.

| Secret | لازم؟ | کاربرد |
|--------|-------|---------|
| `VPS_SSH_KEY` | ✅ اجباری | کلید خصوصی SSH برای اتصال Actions به سرور. **همان کلیدی که در مونوریپو هست** — مالک همان مقدار را در Secrets ریپوی جدید هم می‌گذارد |
| `OWNER_TELEGRAM_ID` | ✅ عملاً اجباری | آی‌دی عددی تلگرام مالک. دو کار می‌کند: (۱) هشدارِ خرابیِ دیپلوی/سلامت به تلگرامش می‌رود، (۲) دیپلوی همین را به‌صورت `ADMIN_IDS` در `.env` می‌گذارد پس ادمینِ محصول هم می‌شود. می‌تواند چند آی‌دی با کاما باشد |
| `<PROJ>_BOT_TOKEN` | اگر ربات دارد | توکن از [@BotFather](https://t.me/BotFather). **هر ربات توکن جدا** |
| `<PROJ>_OPENROUTER_KEY` | اگر LLM دارد | از `openrouter.ai/keys`. **حتماً کلید جدا برای هر پروژه** — فقط این‌طور می‌فهمی هزینه‌ی کدام محصول بالا رفته. کلید فقط یک‌بار نمایش داده می‌شود |
| `<PROJ>_BALE_BOT_TOKEN` | اگر بله دارد | توکن ربات بله (BotFather بله) |
| `BACKUP_PASSPHRASE` | اختیاری | رمزنگاری بکاپ شبانه با gpg |
| `CLOUDFLARE_TUNNEL_TOKEN` | اختیاری | تونلِ نام‌دار وقتی دامنه گرفتی (بند ۱۲) |

**قواعد:**
- نامِ Secret با پیشوندِ پروژه شروع شود، حتی وقتی ریپو فقط یک پروژه دارد. روزی پروژه‌ی
  دوم اضافه می‌شود و آن‌وقت `BOT_TOKEN` خالی یعنی ابهام.
- مقدارِ Secret **هرگز** در لاگ چاپ نشود. چاپِ **نامِ** Secret خالی مجاز و لازم است
  (بند ۵، قاعده‌ی ۵).
- کلیدِ گم‌شده = کلیدِ نو بساز، نه بازیابی. کلیدِ قدیمی را در پنل باطل کن.
- تغییرِ مقدارِ یک Secret نیازی به کامیت ندارد: مقدار را عوض کن و ورک‌فلوی Deploy را
  دستی با `force_all=true` اجرا کن.

---

## ۵) دیپلوی

### ۵-۱) الگوی توصیه‌شده: کد را **push** کن، سرور `git` نزند

مونوریپوی فعلی روی سرور یک کلونِ گیت دارد و `git fetch`/`reset` می‌زند. این به دلایل
تاریخی است و **دو کلاس خرابیِ واقعی** ساخته: تصادمِ دو گیتِ هم‌زمان روی یک کلون، و
درختِ کارِ کثیف که هر دیپلویِ بعدی را برای همیشه می‌شکست (و چون مالک SSH ندارد،
دستی هم پاک نمی‌شد).

برای پروژه‌ی جدید این را تکرار نکن. الگوی درست:

```
git archive HEAD | ssh ubuntu@VPS 'tar -x -C ~/<PROJ>'
```

یعنی رانرِ گیت‌هاب که کدِ کامل را دارد، همان لحظه محتوا را روی سرور پیاده می‌کند.
مزیت‌ها: سرور به هیچ credentialِ گیتی نیاز ندارد، درختِ کار وجود ندارد که کثیف شود،
و اولین دیپلوی خودش پوشه را می‌سازد (بدون هیچ راه‌اندازیِ دستی).

**تنها ایرادش:** فایلی که از ریپو **حذف** شود، روی سرور می‌ماند. اگر حذفِ یک فایل مهم بود
(مثلاً یک اسکریپت خطرناک)، یک خطِ `rm` صریح در دیپلوی بگذار و بعد از تأیید اجرا،
همان خط را بردار (قاعده‌ی ۶).

### ۵-۲) هشت قاعده‌ی آهنینِ دیپلوی

این هشت قاعده هرکدام از یک خرابیِ **واقعی** روی همین سرور آمده‌اند:

**۱. دیپلوی باید انتخابی باشد.** اپی که کدش عوض نشده نباید ری‌استارت شود. هر ری‌استارت
یعنی مرگِ فلوهای درجریانِ کاربر. اگر ریپو چند اپ دارد، تغییرِ مسیر را بسنج و فقط
همان را reload کن.

**۲. `.env` باید تک‌منبع باشد، وگرنه دیپلوی وارد پینگ‌پنگ می‌شود.** تشخیصِ «env عوض شد»
با مقایسه‌ی فایلِ تولیدشده و فایلِ روی دیسک است. اگر بلوکِ دومی بعد از ساختِ `.env` چیزی
به آن اضافه کند که سازنده تولیدش نمی‌کند، **هر** دیپلوی فرق می‌بیند و **هر** مرج،
حتی مرجِ صرفاً مستنداتی، اپ را ری‌استارت می‌کند. هرچه در `.env` می‌نشیند باید داخلِ
همان یک تابعِ سازنده تولید شود.

**۳. بلوکِ «یک‌باره» فقط وقتی یک‌باره است که شرطش هم یک‌باره باشد.** idempotent بودنِ
خودِ اسکریپت کافی نیست. شرط باید **وضعیت را تشخیص بدهد** و بعد از انجامِ کار خودش
false شود. یک بلوکِ مهاجرت که شرطش `[ -f .env ]` بود، در هر مرج `pm2 stop` می‌زد و
یک رباتِ درآمدزا را می‌خواباند.

**۴. بعد از تأییدِ اجرا، بلوکِ یک‌باره را از ورک‌فلو بردار.** بلوکِ باقی‌مانده یک تله‌ی
خفته است: مارکرش روی سرور زندگی می‌کند و بعد از هر restore از بکاپ از بین می‌رود،
پس روزی دوباره اجرا می‌شود روی دیتایی که نباید.

**۵. سکرتی که در `env:` هست ولی در `envs:` نیست، روی سرور خالی می‌رسد و هیچ خطایی
نمی‌دهد.** اکشنِ `appleboy/ssh-action` فقط متغیرهای نام‌برده در `envs:` را می‌فرستد.
یک بار شش سکرت این‌طور جا افتادند، سه ربات بالا نیامدند، و دیپلوی با کدِ **صفر** سبز
تمام شد. قاعده: هر متغیری که اسکریپتِ سرور می‌خواند باید در **هر دو** لیست باشد، و
اگر خالی بود، دیپلوی باید **نامش** را صریح چاپ کند (نه مقدارش).

**۶. `chmod +x` روی سرور یعنی درختِ کثیف.** هر فایلی که دیپلوی اجرایی‌اش می‌کند باید
**در گیت** اجرایی باشد (`git update-index --chmod=+x <path>`)، وگرنه هر دیپلوی چیزی
را عوض می‌کند که دیپلویِ بعدی به‌عنوان «تغییرِ ناخواسته» می‌بیند. ضررش خودِ تغییر نیست،
**بی‌معنا شدنِ هشدار** است.

**۷. کرون را با حفظِ خطوطِ موجود بنویس.** روی سرور کرونِ پروژه‌های دیگر هست.
الگوی امن (و آن `|| true` حیاتی است: روی `crontab` خالی، `grep` با کد ۱ برمی‌گردد و
زیرِ `set -e` کلِ کرون **خالی** نصب می‌شود، آن هم با پیامِ موفقیت):

```bash
LINE="*/10 * * * * PATH=... /home/ubuntu/<PROJ>/tools/job.sh >> /home/ubuntu/<PROJ>-job.log 2>&1"
EXISTING="$(crontab -l 2>/dev/null | grep -v '<PROJ>/tools/job.sh' || true)"
printf '%s\n%s\n' "$EXISTING" "$LINE" | sed '/^$/d' | crontab -
crontab -l | grep -q '<PROJ>/tools/job.sh' || { echo "❌ نصب کرون نشد"; exit 1; }
```

**۸. `npm ci` روی سرور می‌تواند به registry نرسد.** یک تورِ ایمنی بگذار: اگر نصب شکست
خورد، اپِ قبلی را دوباره بالا بیاور و دیپلوی را قرمز کن؛ هرگز اپ را خاموش رها نکن.

### ۵-۳) `deploy.yml` آماده (کپی کن، فقط بالای فایل را عوض کن)

`<PROJ>` = اسمِ kebab-case پروژه (مثل `shop-bot`) · `<PROJ_UPPER>` = همان با حروف بزرگ و
آندرلاین (مثل `SHOP_BOT`)، که پیشوندِ نامِ Secret هاست.

```yaml
name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      force_all:
        description: 'دیپلوی اجباری حتی بدون تغییر'
        type: boolean
        default: false

# ⚙️ فقط این سه خط را برای پروژه‌ی خودت عوض کن
env:
  PROJ: <PROJ>                 # نام پوشه روی سرور و نام اپ pm2
  VPS_HOST: 185.204.171.170
  VPS_USER: ubuntu

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }

      # کدام مسیرها عوض شده‌اند؟ (روی رانر حساب می‌شود، نه روی سرور)
      - id: changed
        env:
          BEFORE: ${{ github.event.before }}
          FORCE:  ${{ github.event.inputs.force_all || 'false' }}
        run: |
          REASON=""
          [ "$FORCE" = "true" ] && REASON="force"
          if [ -z "$REASON" ] && [ "${{ github.event_name }}" = "push" ] \
             && [ -n "$BEFORE" ] && git cat-file -e "$BEFORE" 2>/dev/null; then
            if git diff --name-only "$BEFORE" "${{ github.sha }}" \
               | grep -vE '\.(md|example)$' | grep -q .; then REASON="code"; fi
          else
            [ -z "$REASON" ] && REASON="code"   # fail-open: نمی‌دانیم، پس دیپلوی کن
          fi
          echo "reason=$REASON" >> "$GITHUB_OUTPUT"
          [ -n "$REASON" ] && echo "🚀 دیپلوی (دلیل: $REASON)" || echo "⏭ تغییری نبود"

      - name: Push code + reload
        if: steps.changed.outputs.reason != ''
        env:
          SSH_KEY:      ${{ secrets.VPS_SSH_KEY }}
          BOT_TOKEN:    ${{ secrets.<PROJ_UPPER>_BOT_TOKEN }}
          OR_KEY:       ${{ secrets.<PROJ_UPPER>_OPENROUTER_KEY }}
          OWNER_ID:     ${{ secrets.OWNER_TELEGRAM_ID }}
        run: |
          set -e
          mkdir -p ~/.ssh
          printf '%s\n' "$SSH_KEY" > ~/.ssh/deploy_key && chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H "$VPS_HOST" >> ~/.ssh/known_hosts 2>/dev/null
          SSH="ssh -i $HOME/.ssh/deploy_key -o StrictHostKeyChecking=accept-new $VPS_USER@$VPS_HOST"

          # ⚠️ قاعده‌ی ۵: سکرتِ خالی را صریح نام ببر (هرگز مقدارش را)
          for v in BOT_TOKEN OR_KEY; do
            eval "val=\${$v}"
            [ -n "$val" ] || echo "⚠️ سکرتِ خالی روی رانر: $v"
          done

          # ۱) کد را روی سرور پیاده کن (فایل‌های runtime مثل .env و data/ دست نمی‌خورند)
          $SSH "mkdir -p ~/$PROJ"
          git archive HEAD | $SSH "tar -x -C ~/$PROJ"

          # ۲) .env را از Secrets بساز — تک‌منبع (قاعده‌ی ۲)
          #    هر چیزی که در .env می‌نشیند باید همین‌جا تولید شود، نه در بلوک دوم.
          ENV_CONTENT="$(printf 'BOT_TOKEN=%s\nOPENROUTER_API_KEY=%s\nADMIN_IDS=%s' \
                          "$BOT_TOKEN" "$OR_KEY" "$OWNER_ID")"
          printf '%s\n' "$ENV_CONTENT" | $SSH "umask 077; cat > ~/$PROJ/.env.new
            if [ ! -f ~/$PROJ/.env ] || ! cmp -s ~/$PROJ/.env.new ~/$PROJ/.env; then
              mv ~/$PROJ/.env.new ~/$PROJ/.env; echo '🔑 .env به‌روز شد'
            else rm -f ~/$PROJ/.env.new; fi"

          # ۳) وابستگی‌ها + reload، با تورِ ایمنیِ قاعده‌ی ۸
          $SSH "set -e
            cd ~/$PROJ
            trap 'code=\$?; [ \$code -eq 0 ] && exit 0;
                  echo \"❌ دیپلوی شکست خورد (\$code) — تلاش برای بالا نگه داشتن اپ\";
                  pm2 start ecosystem.config.cjs --only $PROJ 2>/dev/null || true;
                  pm2 ls; exit \$code' EXIT
            npm ci --omit=dev
            pm2 startOrReload ecosystem.config.cjs --update-env --only $PROJ
            sleep 3
            pm2 jlist | node -e \"
              const a=JSON.parse(require('fs').readFileSync(0,'utf8'));
              const p=a.find(x=>x.name==='$PROJ');
              const s=p?p.pm2_env.status:'missing';
              console.log(s==='online'?'✅ $PROJ بالا است':'❌ $PROJ بالا نیست: '+s);
              if(s!=='online') process.exit(1);\"
            # ⚠️ قاعده‌ی ۳ بند ۲: فقط وقتی همه‌ی اپ‌ها سالم‌اند save کن
            if pm2 jlist | node -e \"
                 const a=JSON.parse(require('fs').readFileSync(0,'utf8'));
                 process.exit(a.every(p=>p.pm2_env.status==='online')?0:1);\"; then
              pm2 save
            else
              echo '⚠️ اپی غیر از مالِ ما online نیست — pm2 save عمداً زده نشد'
              pm2 ls
            fi"

      - name: هشدار تلگرام در خرابی
        if: failure()
        env:
          TOKEN: ${{ secrets.<PROJ_UPPER>_BOT_TOKEN }}
          OWNER: ${{ secrets.OWNER_TELEGRAM_ID }}
        run: |
          [ -n "$TOKEN" ] && [ -n "$OWNER" ] || { echo "ℹ️ توکن/آی‌دی نبود"; exit 0; }
          IFS=',' read -ra IDS <<< "$OWNER"
          for id in "${IDS[@]}"; do
            id="$(echo "$id" | tr -d '[:space:]')"; [ -n "$id" ] || continue
            curl -fsS -X POST "https://api.telegram.org/bot${TOKEN}/sendMessage" \
              -d chat_id="$id" \
              --data-urlencode text="🚨 Deploy پروژه‌ی <PROJ> شکست خورد — ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" || true
          done
```

> ⚠️ **معاوضه‌ی آگاهانه در گاردِ `pm2 save`:** این گارد فقط وقتی save می‌زند که **همه‌ی**
> اپ‌های سرور `online` باشند. یعنی اگر رباتِ کسِ دیگری خوابیده باشد، `pm2 save` تو هم
> زده نمی‌شود و اپِ تازه‌ات **بعد از ری‌بوتِ سرور بالا نمی‌آید**. اگر در لاگِ دیپلوی پیامِ
> «pm2 save عمداً زده نشد» را دیدی، بی‌خیالش نشو: اول بفهم کدام اپ خوابیده و به مالک
> خبر بده. سکوت در این نقطه یعنی یک بمبِ ساعتیِ چند ماه بعد.

`ecosystem.config.cjs` کنارش:

```js
// PM2 — اپ‌های این پروژه. نام باید در کلِ سرور یکتا باشد (بند ۲).
module.exports = {
  apps: [
    { name: '<PROJ>', cwd: '.', script: 'index.js' },
  ],
};
```

### ۵-۴) مسیرِ اضطراری وقتی GitHub Actions در دسترس نیست

یک بار کلِ Actions حساب به‌خاطر بیلینگ خوابید و چون تنها راهِ رساندنِ کد به سرور همان
دیپلوی بود، کارِ آماده روی زمین ماند. پس در `tools/manual-deploy.sh` پروژه‌ات یک
اسکریپت بگذار که همان کارِ دیپلوی را **از داخلِ خودِ سرور** انجام دهد (بدون هیچ سکرتی،
چون `.env` از قبل روی سرور است). مالک می‌تواند آن را از **کنسولِ وبِ ارائه‌دهنده‌ی VPS**
اجرا کند بدون اینکه SSH داشته باشد.

---

## ۶) CI

هدفِ CI اینجا «تستِ کامل» نیست، **گرفتنِ خرابیِ احمقانه قبل از رسیدن به سرور** است.
چهار چکِ ارزان که تجربه نشان داده بیشترین بازده را دارند:

```yaml
name: CI
on:
  pull_request:
  workflow_dispatch:      # لازم است: یک بار CI روی هدِ یک PR اصلاً تریگر نشد

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20, cache: npm }
      - run: npm ci
      - name: Syntax check
        run: node --check index.js
      # تستِ دودِ boot: اجرای واقعی تا گاردِ ENV. همه‌ی import ها resolve می‌شوند.
      - name: Boot smoke test
        run: |
          set +e
          OUT=$(BOT_TOKEN= OPENROUTER_API_KEY= node index.js 2>&1); CODE=$?
          set -e
          echo "$OUT"
          [ "$CODE" = "1" ] || { echo "❌ انتظار exit=1 بود، شد $CODE"; exit 1; }
          echo "$OUT" | grep -q "خالی است" || { echo "❌ پیام گاردِ ENV دیده نشد"; exit 1; }
```

نکته‌ی مهم: **CI فقط روی `pull_request` اجرا شود، نه روی `push: main`.** چکِ PR روی
نتیجه‌ی merge اجرا می‌شود، پس اجرای دوباره روی `main` تقریباً همیشه همان درخت را
دوباره تست می‌کند و سهمیه‌ی Actions را دو برابر می‌سوزاند (بند ۱۴).

**گاردِ لازم که `node --check` نمی‌گیرد:** یک شناسه‌ی import‌نشده روی مسیرِ کم‌رفت‌وآمد
(رسید، ریفاند، ادمین، شاخه‌ی خطا) تا لحظه‌ی رسیدنِ کاربرِ واقعی ساکت می‌ماند. یک بار
یک تابعِ import‌نشده تأییدِ خودکارِ رسیدِ یک رباتِ زنده را **یک روز** خواباند. اگر
پروژه‌ات مسیرهای سرد دارد، یک چکِ شناسه‌ی تعریف‌نشده اضافه کن (الگو:
`~/voice2text/tools/check-undefined.mjs` در مونوریپو).

---

## ۷) Ops — دیباگ بدون SSH

مالک SSH ندارد و سشن هم نباید مستقیم SSH بزند. راهِ دیدنِ سرور، یک ورک‌فلوی
`workflow_dispatch` است که سشن با GitHub MCP (`actions_run_trigger`) اجرا می‌کند و
خروجی را با `get_job_logs` می‌خواند.

**وقتی مالک گفت «فلان چیز مشکل دارد» (حتی یک جمله)، بدون سؤالِ اضافه این مسیر را برو:**
`Ops → status` ← `Ops → errors` ← تشخیص ← فیکس ← PR ← merge ← رصدِ Deploy ← دوباره
`Ops → status` برای تأیید.

```yaml
name: Ops
on:
  workflow_dispatch:
    inputs:
      action:
        type: choice
        options: [status, logs, errors, restart, env-check, db-query]
        required: true
      lines:
        default: '100'
      query:
        description: 'db-query: فقط SELECT'
        default: ''

jobs:
  ops:
    runs-on: ubuntu-latest
    steps:
      - uses: appleboy/ssh-action@v1
        env:
          OPS_ACTION: ${{ inputs.action }}
          OPS_LINES:  ${{ inputs.lines }}
          OPS_QUERY:  ${{ inputs.query }}
          PROJ: <PROJ>
        with:
          host: 185.204.171.170
          username: ubuntu
          key: ${{ secrets.VPS_SSH_KEY }}
          # ⚠️ قاعده‌ی ۵ بند ۵: هرچه اسکریپت می‌خواند باید اینجا هم باشد
          envs: OPS_ACTION,OPS_LINES,OPS_QUERY,PROJ
          script: |
            set -e
            case "$OPS_LINES" in (*[!0-9]*|'') OPS_LINES=100;; esac
            cd ~/"$PROJ"
            case "$OPS_ACTION" in
              status)
                pm2 ls
                systemctl list-unit-files 2>/dev/null | grep -q '^pm2-ubuntu' \
                  && echo "pm2 startup: ✅" || echo "pm2 startup: ❌ بعد از ری‌بوت بالا نمی‌آید"
                df -h / | tail -1
                ;;
              logs)
                tail -n "$OPS_LINES" ~/.pm2/logs/"$PROJ"-out.log   2>/dev/null || echo "(خالی)"
                tail -n "$OPS_LINES" ~/.pm2/logs/"$PROJ"-error.log 2>/dev/null || echo "(خالی)"
                ;;
              errors)
                tail -n "$OPS_LINES" ~/.pm2/logs/"$PROJ"-error.log 2>/dev/null || echo "(خالی)"
                ;;
              restart)
                # فقط اپِ خودِ این پروژه — هرگز نامِ اپ را ورودی‌پذیر نکن
                pm2 restart "$PROJ" --update-env && sleep 3 && pm2 ls
                ;;
              env-check)
                # فقط وجودِ فایل — هرگز محتوا
                [ -f .env ] && echo ".env ✅" || echo ".env ❌"
                ;;
              db-query)
                [ -n "$OPS_QUERY" ] || { echo "❌ query لازم است"; exit 1; }
                # اتصال readonly: نوشتن در سطحِ SQLite رد می‌شود
                node -e '
                  const D=require("./node_modules/better-sqlite3");
                  const db=new D("data/bot.db",{readonly:true,fileMustExist:true});
                  const rows=db.prepare(process.env.OPS_QUERY).all();
                  console.log("rows:",rows.length);
                  for(const r of rows.slice(0,200)) console.log(JSON.stringify(r));'
                ;;
            esac
```

⚠️ دو نکته‌ی امنیتی که در نسخه‌ی مونوریپو با خون یاد گرفته شد:
- ورودی کاربر **هرگز** داخل رشته‌ی shell یا SQL جاسازی نشود؛ با `env` پاس داده شود
  (بالا `process.env.OPS_QUERY`، نه interpolation).
- نامِ اپ را از ورودی نگیر یا با الگوی سخت اعتبارسنجی‌اش کن، وگرنه یک ورک‌فلویِ دیباگ
  به ابزارِ ری‌استارتِ ربات‌های دیگران تبدیل می‌شود.
- اگر روزی به یک دستورِ تشخیصیِ خاص نیاز داشتی، یک اکشنِ موقت اضافه کن و **بعد از
  استفاده حذفش کن** (قاعده‌ی ۴ بند ۵).
- **لاگِ جاب روی ریپوی public برای همه خواناست.** `db-query` بالا خروجی را خام چاپ
  می‌کند؛ برای هر کوئری‌ای که متنِ کاربر یا دیتای per کاربر برمی‌گرداند از الگوی
  `db-query-sealed` مونوریپو استفاده کن (خروجی با کلیدِ عمومیِ سشن رمز می‌شود و فقط
  محلی باز می‌شود): `~/voice2text/.github/workflows/ops.yml` + `tools/ops-unseal.mjs`.
- **سقفِ `command_timeout`ِ ssh-action پروسه را نمی‌کشد، فقط اتصال را می‌بندد.**
  پروسه‌ی بدونِ PTY با بسته‌شدنِ اتصال هیچ سیگنالی نمی‌گیرد و روی سرورِ مشترک می‌دود
  (کوئریِ سنگینی که یک هسته و یک snapshotِ SQLite را روی دیتابیسِ رباتِ زنده نگه
  داشت). هر دستورِ بالقوه طولانی را خودت زیرِ `timeout -k 10 <ثانیه>` اجرا کن و سقفش را
  کمتر از `command_timeout` بگذار. برای پاک‌کردنِ جامانده، با **محیطِ** پروسه انتخاب کن
  (`/proc/<pid>/environ`) نه با `pkill -f`: cmdlineِ شلِ اسکریپت خودِ الگو را دارد و
  خودش را می‌کشد. الگو: اکشنِ `kill-query` در همان ops.yml.

---

## ۸) پایش سلامت

### ۸-۱) چه چیزی همین حالا **رایگان** پوشش دارد

روی سرور یک ناظرِ همیشه‌روشن به نام `health-watch` (اپ pm2 مالِ مونوریپو) هر ۵ دقیقه
اجرا می‌شود. این ناظر **روی همه‌ی پروسه‌های pm2** حلقه می‌زند، نه فقط مالِ خودش. یعنی
اپِ پروژه‌ی جدیدِ تو هم از روز اول این‌ها را رایگان می‌گیرد:

- اپ `online` نیست → پیامِ تلگرام به مالک
- اپ در کرش‌لوپ است → پیامِ تلگرام
- دیسکِ سرور بالای ۹۲٪ → پیامِ تلگرام
- وقتی مشکل رفع شد → پیامِ «✅ رفع شد»

هشدارها فقط روی **تغییرِ حالت** می‌روند (نه هر ۵ دقیقه) و مشکلِ پابرجا هر ۶ ساعت
یادآوری می‌شود.

> پیام از توکنِ رباتِ مونوریپو فرستاده می‌شود، پس هشدارِ پروژه‌ی تو ممکن است از رباتِ
> دیگری برسد. این عمدی و بی‌ضرر است.

⚠️ **این پوشش را با «پایشِ کامل» اشتباه نگیر.** یک بار یک پروسه‌ی بی‌صاحب روی همین سرور
۹۹۱ بار ری‌استارت شد و ۳۳٪ CPU خورد بدونِ اینکه هیچ هشداری برود: هر بار که ناظر نگاه
می‌کرد، همان لحظه `online` بود. یعنی اپی که سریع کرش می‌کند و سریع بالا می‌آید می‌تواند
از لای این گارد رد شود. تنها چیزی که آن را لو داد، جدولِ `pm2 ls` در لاگِ یک دیپلوی بود.

### ۸-۲) چه چیزی پوشش **ندارد** (و باید خودت اضافه کنی)

- **«اپ اصلاً در pm2 نیست»**: ناظر برای این چک، لیستِ اپ‌ها را از `ecosystem.config.cjs`
  **مونوریپو** می‌خواند. اپِ پروژه‌ی تو آنجا نیست، پس اگر بعد از ری‌بوتِ سرور بالا نیاید
  (یا کسی `pm2 delete` کند) **هیچ‌کس خبردار نمی‌شود**.
- سرویسِ systemd پروژه‌ات (اگر داری).
- موجودیِ کلیدِ OpenRouterِ پروژه‌ات.
- خرابیِ منطقی (اپ `online` است ولی محصول کار نمی‌کند).

### ۸-۳) `health.yml` آماده (ناظرِ بیرونی، هر ۶ ساعت)

```yaml
name: Health
on:
  schedule:
    - cron: '23 */6 * * *'   # دقیقه‌ی غیرگرد عمدی است؛ صفِ گیت‌هاب سرِ ساعت شلوغ است
  workflow_dispatch:
permissions: { contents: read, issues: write }

jobs:
  health:
    runs-on: ubuntu-latest
    steps:
      - id: check
        env:
          SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
        run: |
          set +e
          mkdir -p ~/.ssh
          printf '%s\n' "$SSH_KEY" > ~/.ssh/k && chmod 600 ~/.ssh/k
          ssh-keyscan -H 185.204.171.170 >> ~/.ssh/known_hosts 2>/dev/null
          OUT=$(ssh -i ~/.ssh/k -o StrictHostKeyChecking=no -o ConnectTimeout=20 \
                ubuntu@185.204.171.170 'bash -s' <<'REMOTE'
            pm2 jlist 2>/dev/null | node -e "
              let a=[]; try{a=JSON.parse(require('fs').readFileSync(0,'utf8'))}catch(e){}
              const p=a.find(x=>x.name==='<PROJ>');
              if(!p) console.log('PROBLEM:🔴 اپ «<PROJ>» اصلاً در pm2 نیست (ری‌بوتِ بدون resurrect؟)');
              else if(p.pm2_env.status!=='online') console.log('PROBLEM:🔴 اپ «<PROJ>» آفلاین است ('+p.pm2_env.status+')');
            "
            echo "HEALTH_DONE"
          REMOTE
          )
          RC=$?; echo "$OUT"
          if [ $RC -ne 0 ] || ! printf '%s' "$OUT" | grep -q HEALTH_DONE; then
            echo "alert=🔴 سرور در دسترس نیست (rc=$RC)" >> "$GITHUB_OUTPUT"; exit 0
          fi
          A=$(printf '%s\n' "$OUT" | sed -n 's/^PROBLEM://p')
          { echo "alert<<EOF"; printf '%s\n' "$A"; echo "EOF"; } >> "$GITHUB_OUTPUT"

      - name: هشدار تلگرام
        if: steps.check.outputs.alert != ''
        env:
          TOKEN: ${{ secrets.<PROJ_UPPER>_BOT_TOKEN }}
          OWNER: ${{ secrets.OWNER_TELEGRAM_ID }}
          A:     ${{ steps.check.outputs.alert }}
        run: |
          [ -n "$TOKEN" ] && [ -n "$OWNER" ] || exit 0
          URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          # ⚠️ خطِ دوم با $'\n' ساخته می‌شود، نه با newline خام:
          #    newline خام در ستون صفر، بلوکِ YAML را می‌بندد و فایل نامعتبر می‌شود.
          MSG="🚨 <PROJ>: $A"$'\n'"لاگ: $URL"
          IFS=',' read -ra IDS <<< "$OWNER"
          for id in "${IDS[@]}"; do
            curl -fsS -X POST "https://api.telegram.org/bot${TOKEN}/sendMessage" \
              -d chat_id="$(echo "$id"|tr -d '[:space:]')" \
              --data-urlencode text="$MSG" || true
          done

      - name: قرمزکردن جاب
        if: steps.check.outputs.alert != ''
        run: exit 1
```

**قاعده‌ی هشدار (مهم‌تر از خودِ هشدار):** هشداری که مدام شلیک کند، بی‌معنا می‌شود و
خرابیِ واقعی لای نویزش گم می‌شود. هر بار که یک هشدارِ کاذب دیدی، **همان لحظه** یا
شرطش را دقیق کن یا حذفش کن. هشدارِ کمتر و درست، بی‌نهایت بهتر از هشدارِ زیاد است.

---

## ۹) بکاپ دیتابیس

بکاپ **شاملِ پروژه‌ی تو نمی‌شود** مگر خودت اضافه کنی: ورک‌فلوی بکاپِ مونوریپو فقط
`~/voice2text/bots/*/data/*.db` و `~/tabir_khab/*.db` را می‌گیرد.

اگر پروژه‌ات دیتای کاربر یا پول دارد، این را **قبل از اولین کاربر واقعی** راه بینداز.
روشِ درست، snapshotِ آنلاینِ SQLite است (نه `cp`، که وسطِ نوشتن فایلِ خراب می‌دهد).
پایتون روی سرور هست و به هیچ وابستگیِ اضافه‌ای نیاز ندارد:

```yaml
name: Backup
on:
  schedule:
    - cron: '41 2 * * *'    # ~۰۶:۱۱ تهران — با بکاپِ مونوریپو (۰۲:۴۷ UTC) تصادم نکند
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Snapshot + fetch
        env:
          SSH_KEY: ${{ secrets.VPS_SSH_KEY }}
        run: |
          set -e
          mkdir -p ~/.ssh
          printf '%s\n' "$SSH_KEY" > ~/.ssh/k && chmod 600 ~/.ssh/k
          O="-i $HOME/.ssh/k -o StrictHostKeyChecking=accept-new"
          H="ubuntu@185.204.171.170"
          # ⚠️ پایتون عمداً یک‌خطی است: خطِ دوم در ستون صفر بلوکِ YAML را می‌بندد،
          #    و heredocِ تودرتو هم با تورفتگیِ YAML به IndentationError می‌خورد.
          ssh $O $H 'set -e
            S=$HOME/<PROJ>-backup-stage; rm -rf $S; mkdir -p $S
            n=0
            for f in $HOME/<PROJ>/data/*.db; do
              [ -e "$f" ] || continue
              python3 -c "import sqlite3,sys; s=sqlite3.connect(\"file:\"+sys.argv[1]+\"?mode=ro\",uri=True); d=sqlite3.connect(sys.argv[2]); s.backup(d); d.close(); s.close(); print(\"snapshot ok: \"+sys.argv[1])" "$f" "$S/$(basename $f)"
              n=$((n+1))
            done
            [ $n -gt 0 ] || { echo "❌ هیچ دیتابیسی پیدا نشد"; exit 1; }'
          scp $O -r "$H:<PROJ>-backup-stage" ./snap
          ssh $O $H 'rm -rf $HOME/<PROJ>-backup-stage'

      - name: Pack (+رمز اگر BACKUP_PASSPHRASE ست باشد)
        env:
          P: ${{ secrets.BACKUP_PASSPHRASE }}
        run: |
          tar -czf backup-$(date -u +%F).tar.gz snap
          if [ -n "$P" ]; then
            gpg --batch --yes --passphrase "$P" -c backup-*.tar.gz && rm backup-*.tar.gz
          fi

      - uses: actions/upload-artifact@v4
        with:
          name: <PROJ>-db-backup
          path: backup-*
          retention-days: 30
```

**بازگردانی:** artifact را از صفحه‌ی run دانلود کن (رمز: `gpg -d`). چون مالک SSH ندارد،
جایگزینی از مسیرِ یک بلوکِ **یک‌باره** در دیپلوی انجام می‌شود (و طبق قاعده‌ی ۴ بند ۵،
بعد از تأیید برداشته می‌شود). قبل از هر restore حتماً اول اپ را `pm2 stop` کن.

---

## ۹ب) اتصال اختیاری به داشبورد ادمینِ موجود (و قراردادِ آنالیتیکس)

روی همین سرور یک **داشبورد ادمینِ وب** هست (اپ pm2 به نام `dashboard`) که دیتای
محصولات را **readonly** می‌خواند و اتریبیوشن، قیف، مالی و پشتیبانی را نشان می‌دهد.
پروژه‌ی جدید می‌تواند از روز اول داخلش دیده شود، **اگر** از ابتدا قرارداد را رعایت کند.
اضافه‌کردنش بعداً به دیتای گذشته برنمی‌گردد، پس این تصمیم را **قبل از اولین کاربر** بگیر.

**قراردادی که باید در پروژه‌ی جدید رعایت شود:**

```sql
-- جدول کاربران: کلید اصلی telegram_id ، created_at از نوع unix
CREATE TABLE IF NOT EXISTS users (
  telegram_id INTEGER PRIMARY KEY,
  name        TEXT,
  created_at  INTEGER DEFAULT (unixepoch())
);
-- جدول رویدادها: در همه‌ی محصولات دقیقاً همین شکل است (نسخه‌ی قرارداد = 3)
CREATE TABLE IF NOT EXISTS events (
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id    INTEGER,
  event      TEXT    NOT NULL,
  props      TEXT    NOT NULL DEFAULT '{}',
  created_at INTEGER NOT NULL DEFAULT (unixepoch())
);
CREATE INDEX IF NOT EXISTS idx_events_user  ON events(user_id, created_at);
CREATE INDEX IF NOT EXISTS idx_events_event ON events(event, created_at);
-- اتریبیوشن write-once روی users
ALTER TABLE users ADD COLUMN first_source  TEXT NOT NULL DEFAULT '';
ALTER TABLE users ADD COLUMN first_payload TEXT NOT NULL DEFAULT '';
ALTER TABLE users ADD COLUMN first_version TEXT NOT NULL DEFAULT '';
```

- `db.pragma('busy_timeout = 5000')` بگذار: داشبورد نویسنده/خواننده‌ی دوم است و نباید
  فوری `SQLITE_BUSY` بگیرد. `journal_mode = WAL` هم روشن باشد.
- نامِ رویدادها را از واژه‌نامه‌ی مشترک بردار (`start`, `onboard_done`, `first_value`,
  `paywall_shown`, `recharge_started`, `receipt_submitted`, `payment_approved`,
  `payment_rejected`, `product_delivered`, `refund`, `feedback`, `reset`). رویدادِ
  اختصاصی مجاز است (`snake_case`) ولی نباید هم‌معنیِ یکی از این‌ها باشد.
- **ثبتِ رویداد باید fail-safe باشد**: هر خطا فقط لاگ شود و هرگز فلوی کاربر را نشکند.
- اگر کیف‌پول داری، جدول `payments` با ستون‌های `amount` / `status`
  (`waiting_review` → `approved`) / `created_at` (unix) بساز تا صفحه‌ی مالی خودکار کار کند.

**⚠️ قواعدِ تغییرناپذیری:** نامِ رویداد یا propهای موجود **هرگز** عوض یا حذف نشوند؛ فقط
اضافه کن. تحلیلِ تاریخی و داشبورد با تغییرشان بی‌صدا می‌شکند.

**نکته‌ی cross-repo:** کدِ مشترکِ آنالیتیکس در مونوریپو با import نسبی استفاده می‌شود، پس
ریپوی جدید نمی‌تواند importش کند. یک کپیِ کوچک بردار و یک چکِ ساده بگذار که کپی با
قرارداد واگرا نشود (همان کاری که `voice2text` می‌کند). و دیده‌شدن در داشبورد نیاز به یک
**ردیفِ رجیستری در ریپوی مونوریپو** دارد (`bots/dashboard/lib/bots.js`) با `dataDir`
مطلق به `/home/ubuntu/<PROJ>/data` — یعنی یک PR جدا در آن ریپو. تا وقتی این ردیف اضافه
نشده، داشبورد پروژه‌ی تو را نمی‌بیند و این کاملاً بی‌خطر است.

---

## ۱۰) OpenRouter و قواعد هزینه‌ی LLM

**نقطه‌ی ورود:** `https://openrouter.ai/api/v1/chat/completions` با هدرِ
`Authorization: Bearer <کلید همان پروژه>`. موجودی: `https://openrouter.ai/api/v1/credits`.

**مدل‌های پیش‌فرضِ این مجموعه:**

| کار | مدل |
|-----|------|
| پیش‌فرض (سریع و ارزان) | `google/gemini-2.5-flash` |
| کارهای دقیق | `google/gemini-2.5-pro` |
| فالبکِ ارزان | `deepseek/deepseek-v3.2` |

**قواعد آهنین (هرکدام از یک هزینه‌ی واقعی آمده):**

1. **هیچ فراخوانی بدون retry + مدل فالبک + اعتبارسنجیِ خروجی.** الگوی جاافتاده:
   سه تلاش با مدل اصلی، دو تلاش با فالبک، و یک `validate` اختیاری که خروجیِ خراب را رد
   کند. اگر همه شکست خوردند، `null` برگردان و **حقِ کاربر را کامل حفظ کن** (پول/اشتراک
   برگردد) با یک پیامِ صادقانه و دکمه‌ی تلاش مجدد.
2. **هیچ فراخوانیِ پولی قبل از کسرِ اعتبار.** فراخوانی باید **بلافاصله بعد از** نقطه‌ی
   تصمیمِ کاربر باشد، نه قبلش. یک بار یک ربات قبل از پی‌وال دو ریکوئست می‌زد، یعنی هزینه‌ی
   کاملِ هر کاربری که منصرف می‌شد روی دستِ ما بود. و «توانِ پرداخت» با «تصمیمِ پرداخت»
   یکی نیست: کاربرِ باموجودی هم ممکن است منصرف شود.
3. **گارد باید در خودِ تابعِ فراخوانی باشد، نه سرِ هر مسیر.** یک شرط روی وضعیتِ رکورد
   کلِ قاعده را قفل می‌کند و مسیرهای **آینده** را هم می‌گیرد؛ گاردِ پراکنده دیر یا زود
   یک مسیر را جا می‌گذارد.
4. **هر مسیرِ رایگانی که LLM صدا می‌زند باید سقف داشته باشد**: سقفِ حجم/طولِ ورودی،
   سقفِ دفعات در روز، و کشِ دائمیِ نتایجِ تکرارشونده. هزینه قبل از درآمد کنترل می‌شود.
5. **ورودی کاربر data است، نه instruction.** هر متنی که از کاربر وارد پرامپت می‌شود
   باید با گاردِ ضدِ تزریق بسته‌بندی شود.
6. **ورودیِ چندرسانه‌ای در یک فراخوانی، نه دو تا.** مدل‌های Gemini خودِ فایلِ صوتی را
   به‌عنوان `input_audio` می‌گیرند؛ «اول رونویسی، بعد ارسالِ متن» دو برابر هزینه و تأخیر
   است بدون فایده. (حواست باشد مدلِ فالبکِ متنی صدا نمی‌فهمد، پس برنامه‌ی retry برای
   ورودی صوتی فقط باید مدل‌های شنوا داشته باشد.)
7. **خروجیِ ساخت‌یافته را مقاوم پارس کن**: حذفِ code fence و برشِ از اولین `{` تا آخرین
   `}` قبل از `JSON.parse`.
8. **هزینه‌ی واقعی را از خودِ پاسخ بخوان، نه از جدولِ قیمتِ هاردکد.** OpenRouter هزینه‌ی
   هر فراخوانی را برمی‌گرداند؛ هر جدولِ قیمتی که در کد نگه داری، کهنه می‌شود و بی‌صدا
   دروغ می‌گوید. اگر حسابداری اضافه می‌کنی: بدنه‌ی ریکوئست بایت‌به‌بایت دست‌نخورده بماند،
   ثبت بعد از استخراجِ متن و داخلِ `try/catch`، و یک کلیدِ خاموشیِ یک‌خطی داشته باشد.

**تفکیکِ هزینه:** چون کلیدِ هر پروژه جداست، بالا رفتنِ ناگهانیِ مصرفِ یک کلید یعنی باگ یا
سوءاستفاده. این را جدی بگیر.

---

## ۱۱) اگر پروژه ربات تلگرام یا بله دارد

**تلگرام:** `https://api.telegram.org` · کتابخانه‌ی مرسوم در این مجموعه: `telegraf`
(Node) با **long polling**. long polling یعنی هیچ پورتِ ورودی روی سرور باز نمی‌شود؛
این پیش‌فرضِ درست است، سراغِ webhook نرو مگر دلیلِ محکمی داشته باشی.

**بله:** `https://tapi.bale.ai` — شکلِ Bot API تقریباً همان تلگرام است، پس یک کلاینتِ
پارامتری برای هر دو کافی است و نباید کد فورک شود. دو تفاوتِ عملی که در این مجموعه
دیده شده: گواهیِ SSL نیاز به تنظیمِ خاص دارد، و پرداخت از مسیرِ کیف‌پولِ بله با توکنِ
جدا انجام می‌شود.

**اصولی که هر رباتِ این مجموعه رعایت می‌کند:**

1. **`bot.catch` سراسری + هندلرهای `unhandledRejection` و `uncaughtException`.** هرگز
   فلو بی‌صدا نمیرد. کرشِ کنترل‌نشده یعنی مرگِ همه‌ی فلوهای درجریانِ همه‌ی کاربران.
2. **گاردِ ENV در خطِ اول:** اگر توکن یا کلید خالی بود، پیامِ «... خالی است» چاپ و
   `process.exit(1)`. تستِ دودِ CI دقیقاً روی همین بنا شده.
3. **لاگ = stdout/stderr.** pm2 آن را در `~/.pm2/logs/<app>-{out,error}.log` نگه می‌دارد
   و `Ops → logs` همان را می‌خواند. لاگرِ جدا نساز.
4. **حالت (state) در دیتابیس، نه در حافظه.** هر دیپلوی یعنی ری‌استارت؛ فلویِ in-memory
   وسطِ کار می‌میرد. اگر آگاهانه in-memory انتخاب کردی، TTL بگذار و پیامِ انقضای شفاف بده.
5. **از هر صفحه یک راهِ خروجِ صریح** به منوی اصلی باشد. هیچ صفحه‌ای بن‌بست نیست.
   استثنا: صفحه‌ای که از کاربر می‌خواهد **بنویسد یا ویس بفرستد** نباید دکمه داشته باشد
   (دکمه حواسش را پرت می‌کند و فکر می‌کند تنها اقدامِ ممکن همان است).
6. **گاردِ «فلوی باز»:** اگر کاربر وسطِ یک فلوی نیمه‌کاره (فاکتور، پردازش) دکمه‌ی دیگری
   زد، فلو را بی‌صدا رها نکن؛ دو گزینه بده: «ادامه می‌دم» و «انصراف».
7. **هر callback باید در برابرِ دوبار زدن و کهنه بودن ایمن باشد** (قفلِ سینکرون قبل از
   اولین `await`) و **مالکیتِ رکورد** را چک کند.
8. **الگوهای callback را حذف/تغییر نده.** دکمه‌های inline در چتِ کاربران می‌مانند؛
   الگوی قدیمی باید حداقل یک «این دکمه منقضی شده» مؤدبانه بگیرد.
9. **یک اکشن = یک معنی.** اگر معنیِ یک callback عوض شد، اکشنِ **جدید** بساز. یک بار
   دکمه‌ی «انصراف» به اکشنی وصل بود که معنی‌اش عوض شده بود و کاربر در حلقه‌ی بی‌پایان
   گیر کرد، بی‌هیچ خطایی، تا وقتی تیکت داد.
10. **کیبوردِ ماندگار روی گوشیِ کاربر ذخیره است و هیچ متدی از سمتِ سرور تازه‌اش
    نمی‌کند.** یعنی دکمه‌ی جدیدی که اضافه می‌کنی ممکن است هفته‌ها به کاربرِ فعلی نرسد،
    **بدون هیچ خطایی**. اگر منوی پایین داری، یک شماره‌ی نسخه‌ی کیبورد نگه دار و وقتی
    عقب بود، یک پیامِ بی‌صدا با کیبوردِ تازه بفرست و بلافاصله حذفش کن.
11. **محدودیت و ادب:** برای هر ورودیِ کاربر سقف بگذار (حجم فایل، طولِ متن، تعدادِ
    درخواستِ هم‌زمان)، سقف‌ها را بالاتر از مصرفِ کاربرِ عادی تنظیم کن، و در برخورد
    مؤدبانه و شفاف پیام بده.
12. **دکمه‌ی پشتیبانی همیشه در دسترس باشد** و هرگز پشتِ گارد نرود؛ راهِ فرارِ کاربرِ
    گیرکرده باید همیشه باز باشد.

---

## ۱۲) پروژه‌های وب: سایت، API، ورکر

**قاعده‌ی اول: سرویس فقط روی `127.0.0.1` گوش می‌دهد.** هیچ پورتی روی اینترنت باز نمی‌شود.

```js
server.listen(PORT, '127.0.0.1', () => log(`✅ listening on 127.0.0.1:${PORT}`));
```

**رساندنش به بیرون — سه مسیر، به ترتیبِ ترجیح:**

| مسیر | چه وقت | نکته |
|------|---------|------|
| **Cloudflare quick tunnel** | همین حالا، بدون دامنه | آدرسِ `*.trycloudflare.com` که **بعد از هر ری‌استارت عوض می‌شود**. مناسبِ ابزارِ داخلی و تست، **نامناسب برای محصولِ عمومی** (لینک می‌میرد، سئو ندارد). داشبوردِ فعلی دقیقاً همین است و آدرسِ جدید بعد از هر ری‌استارت به تلگرامِ مالک پیام می‌شود |
| **Cloudflare named tunnel** | وقتی دامنه گرفتی | آدرسِ ثابت روی دامنه‌ی خودت، بدون بازکردنِ هیچ پورتی، SSL خودکار. فقط یک Secret به نام `CLOUDFLARE_TUNNEL_TOKEN` لازم دارد. **این مسیرِ درست برای محصولِ عمومی است** |
| **nginx + گواهیِ SSL روی خودِ سرور** | فقط اگر دلیلِ خاصی برای دوری از کلادفلر داری | نیازمندِ بازکردنِ پورت ۸۰/۴۴۳، نصب و تمدیدِ گواهی، و مراقبتِ امنیتیِ بیشتر روی سروری که ربات‌های زنده رویش‌اند. فرض نکن nginx نصب است؛ اول تأیید بگیر |

**راه‌اندازیِ تونل از داخل دیپلوی** (همان الگویی که برای داشبورد کار می‌کند): یک
سرویسِ systemd بساز که `cloudflared` را با `Restart=always` نگه دارد، و نامش را
یکتا بگذار (`<PROJ>-tunnel`). `cloudflared` از قبل روی سرور نصب است.

**قاعده‌ی امنیتیِ هر چیزی که از تونل بیرون می‌آید:**
- ورود با توکنِ بلندِ تصادفی (≥۳۲ کاراکتر) از Secrets، کوکیِ `HttpOnly` + `SameSite=Strict`.
- چکِ `Origin` روی هر POST.
- هر نوشتن و هر export لاگِ حسابرسی داشته باشد.
- ورودی کاربر هرگز در SQL یا HTML جاسازی نشود (prepared statement و escape).

**ورکر/کرونِ دوره‌ای:** اگر کاری باید هر N دقیقه اجرا شود، **روی سرور** اجرایش کن (یک
اپِ pm2 با حلقه‌ی داخلی، یا کرونِ کاربر با الگوی امنِ قاعده‌ی ۷)، نه با
`schedule` در GitHub Actions. دلیلش در بند ۱۴ است و عدد دارد.

---

## ۱۳) پروژه‌های پایتون

pm2 برای Node است. الگوی اثبات‌شده‌ی پایتون در این زیرساخت: **venv + systemd**.

- مسیر روی سرور: `~/<PROJ>/` با `.venv/` داخلش.
- دیپلوی: همان `git archive | tar -x` (بند ۵-۱)، بعد
  `.venv/bin/pip install -q -r requirements.txt`، بعد `sudo systemctl restart <PROJ>`.
- فایل‌های runtime (`.env`، `*.db`، `logs/`، `.venv/`) دستِ overlay نمی‌خورند. ولی
  فایلی که از ریپو **حذف** شود روی سرور می‌ماند (بند ۵-۱).
- CI: `setup-python` + `pip install -r requirements.txt` + `python -m compileall -q .`
- ⚠️ `Ops` که با pm2 کار می‌کند این سرویس را **نمی‌بیند**. برای پایتون یک ورک‌فلوی
  لاگِ جدا لازم داری: `systemctl status <PROJ>` + `journalctl -u <PROJ> -n 200`.
- ⚠️ ناظرِ سلامتِ مونوریپو فقط دو سرویسِ systemd مشخص را می‌پاید (`tabir-khab` و
  `dash-tunnel`). سرویسِ تو در آن لیست **نیست**، پس چکِ systemd را حتماً به
  `health.yml` خودت اضافه کن.

---

## ۱۴) سهمیه‌ی GitHub Actions (این بند پول است، نه سلیقه)

- ریپوهای **private** سهمیه‌ی ماهانه‌ی ثابتی دارند (~۲۰۰۰ دقیقه). سهمیه **per حساب**
  است، نه per ریپو. یعنی ریپوی جدید از همان کاسه می‌خورد.
- گیت‌هاب **per جاب** و **رند به بالا** حساب می‌کند: جابِ ۱۷ ثانیه‌ای = یک دقیقه‌ی کامل.
  پس **گران‌ترین چیز، تعدادِ جاب است نه طولِ جاب**. یک ماتریسِ ۶تایی برای یک تغییرِ
  تک‌خطی، شش دقیقه خرج می‌کند.
- اندازه‌گیریِ واقعی روی همین حساب: ۱۰۰۰ اجرا و **۲۳۰۷ جاب** در ۲۸ روز ≈ ۲۳۹۰ دقیقه
  در برابر سهمیه‌ی ۲۰۰۰. سهمیه یک بار واقعاً تمام شد و همه‌ی جاب‌ها با صفر ثانیه مردند.
- **کرونِ کوتاه قاتلِ سهمیه است:** یک پایشِ هر ۳۰ دقیقه‌ای به‌تنهایی ~۱۴۴۰ دقیقه در ماه
  می‌خورد (۷۲٪ سهمیه). و بدترش: زمان‌بندیِ گیت‌هاب **تضمینی نیست** (روی همین حساب ۱ تا
  ۴ ساعت تأخیر دیده شده)، یعنی گران‌ترین چیز، دیرترین هشدار را می‌داد.

**قواعد:**
1. **هر کاری که سرور خودش می‌تواند بکند، روی سرور انجام شود.** Actions فقط برای کاری
   است که ذاتاً باید بیرون از سرور باشد: دیپلوی، بکاپِ خارج از سرور، ناظرِ بیرونی.
2. CI فقط روی `pull_request`، نه روی `push: main` (وگرنه هر مرج دو برابر).
3. `cron` در GitHub Actions همیشه **UTC** است (تهران = UTC+۳:۳۰). و دقیقه‌ی گرد نگذار،
   چون صفِ زمان‌بندیِ گیت‌هاب سرِ ساعت شلوغ‌ترین حالت را دارد.
4. قبل از افزودنِ هر ورک‌فلو یا کرونِ جدید، هزینه‌ی ماهانه‌اش را تخمین بزن:
   `تعدادِ اجرا در ماه × تعدادِ جاب`. بالای ~۱۰۰ دقیقه در ماه باید توجیه داشته باشد.
5. **هر ادعای صرفه‌جویی باید روی تاریخچه‌ی واقعی سنجیده شود، نه تخمین زده شود.** یک
   بار یک فیلترِ «هوشمند» اضافه شد که روی ۴۹ کامیتِ واقعی **صفر درصد** صرفه‌جویی داشت.
6. **افزایشِ spending limit و واردکردنِ اطلاعاتِ پرداخت منتفی است.** هرگز پیشنهاد نده.
   تنها راهِ حل، کم‌کردنِ مصرف است. (مالک گاهی ریپو را موقتاً public می‌کند چون
   ریپوی public دقیقه‌ی نامحدود دارد؛ این تصمیمِ او است، بحث ندارد، و یادآوریِ
   private کردنِ دوباره در پایانِ کار یک خطِ کوتاه است نه یک بحث.)

---

## ۱۵) قواعدِ مهندسی که با خون نوشته شده‌اند

این‌ها از باگ‌های **واقعیِ** همین زیرساخت آمده‌اند. تکرارشان گران است.

### ۱۵-۰) اول حذف، بعد تعمیر
اول **هدف** را در یک جمله بگو، بعد تصمیم بگیر:
- چیزی که ما را از هدف **دور** می‌کند: حذف.
- چیزی که ما را به هدف **نزدیک نمی‌کند** ولی نگهداری می‌خواهد و باگ می‌دهد: **باز هم
  حذف**، نه تعمیر. تعمیرِ چیزی که ارزشی اضافه نمی‌کند فقط بدهیِ فنی را گران‌تر می‌کند.
- فقط چیزی که مستقیماً به هدف نزدیک می‌کند ارزشِ تعمیر دارد.

**نشانه‌ی اینکه در دامِ تعمیر افتاده‌ای: وصله روی وصله.** الگوی واقعی: مدل یک فیلد را
اشتباه می‌ساخت → داده اضافه کردیم؛ نادیده گرفت → قاعده‌ی پرامپت اضافه کردیم؛ دور زد →
قاعده را سراسری کردیم. سه لایه وصله، و آخرش معلوم شد کاربر اصلاً آن فیلد را نمی‌خواست.
حذفِ خودِ داده هر سه لایه را با هم برداشت. **هر بار که داری لایه‌ی دومِ وصله را می‌نویسی،
بایست و بپرس این تکه اصلاً باید باشد؟**

### ۱۵-۱) پول مقدس‌ترین چیز است
- کسرِ اعتبار یا **فقط بعد از موفقیت**، یا **اتمیک با گارد** (`UPDATE ... WHERE balance >= price`)
  قبل از اولین `await`. شکستِ سرویس بعد از کسر = **جبرانِ خودکار + امکانِ تلاش مجدد**.
  پولِ کاربر هرگز در حالتِ نامعلوم نمی‌ماند؛ این حقِ اوست، نه لطف.
- منبعِ حقیقتِ پول فقط دیتابیس است، هرگز حافظه. هر ریال ورودی/خروجی ردپای DB دارد.
- **واحدِ پول هرگز دستِ LLM نیست.** یک بار تبدیلِ ریال به تومان به عهده‌ی مدل بود؛ مدل
  عددِ چاپ‌شده را اشتباه گزارش کرد و ربات سه پرداختِ یک‌دهمی را **خودکار تأیید کرد**.
  قاعده: **مدل فقط می‌خواند، کد حساب می‌کند** — مدل عددِ خام + واحدِ خام بدهد و تبدیل
  در کدِ قابل‌تست انجام شود.
- هر گاردِ مبلغ باید **هر دو جهت** را ببیند: ردِ اشتباه و **تأییدِ اشتباه**. نبودِ جهتِ
  دوم همان باگ را ساخت.
- **هر جا عدد و واحد از دو منبعِ مختلف می‌آیند، یکی‌شان دیر یا زود عوض می‌شود و آن یکی
  ساکت می‌ماند.** عددِ نمایشی باید از همان منبعی بیاید که واقعاً کسر می‌شود. اگر قیمت در
  دسترس نبود، **هیچ عددی چاپ نشود**.
- هر تغییرِ منطقِ پول باید سه سؤال را جواب بدهد: ری‌استارت وسطِ عملیات؟ دوبار کلیک؟
  ورودیِ خصمانه؟

### ۱۵-۲) دیتای کاربر مقدس است
هیچ حذف یا اصلاحِ دسته‌جمعی روی دیتابیسِ زنده بدونِ تصمیمِ صریحِ مالک + بکاپِ همان لحظه.
الگوی درست: اول `VACUUM INTO` بکاپ، بعد تغییر، با مارکرِ یک‌باره.

### ۱۵-۳) مهاجرتِ schema فقط افزایشی
`ALTER TABLE ... ADD COLUMN` با `DEFAULT`. **هرگز** DROP یا RENAME ستون/جدول، و هرگز
تغییرِ معنا یا واحدِ یک ستونِ موجود. کدِ جدید باید روی دیتابیسِ قدیمی boot شود، نه برعکس.

### ۱۵-۴) گاردی که با «نبودِ قرمز» سبز می‌شود، هیچ چیز ثابت نمی‌کند
یک گاردِ متنی وقتی سبز است دو معنیِ کاملاً متفاوت دارد: «چیزی نیست» یا «چیزی را که هست
نمی‌بینم». تنها راهِ تفکیک این است که گارد را روی یک نمونه‌ی **واقعیِ شناخته‌شده‌ی نقض**
بزنی و ببینی قرمز می‌شود. هر الگوی جدیدی که به یک گارد اضافه می‌کنی باید کنارش یک
نمونه‌ی مثبت داشته باشد.

و **پهن‌کردنِ گارد رایگان نیست**: گاردی که مدام قرمزِ کاذب می‌دهد همان‌قدر بی‌فایده است
که گاردی که هیچ نمی‌گوید. گاردِ پروداکشن فقط شکل‌های بدونِ ابهام را بگیرد.

### ۱۵-۵) اول ابزار مقصر است، بعد مدل
در یک کارِ واقعیِ همین مجموعه، از ۱۳ باگِ پیداشده **۱۱ تایشان در ابزار و گاردهای خودمان
بود**، نه در مدل یا پرامپت. و چند بار نتیجه را کاملاً **وارونه** کرده بودند (مدلی که ده
دور «برنده» اعلام شده بود، با سنجه‌ی درست بدتر درآمد).

**قاعده:** هر نتیجه‌ی غافلگیرکننده **اول** فرضِ «سنجه‌ام خراب است» را می‌گیرد. راهِ عملی:
چند نمونه‌ی خامِ فلگ‌شده را با چشم بخوان. اگر نمونه‌ها درست به نظر می‌رسند، باگ در سنجه است.

### ۱۵-۶) از هیچ منبعی نقل نکن که بازش نکرده‌ای
نوشتنِ «طبق صفحه‌ی X» وقتی X باز نشده **ممنوع** است، حتی اگر مطمئن باشی. اگر صفحه باز
نشد (پراکسیِ این محیط خیلی چیزها را می‌بندد)، یا ادعا را ننویس یا با برچسبِ صریحِ
**«خوانده‌نشده»** بنویس. **هم‌رأییِ چند منبعِ ثانویه تأیید نیست.**
یک بار یک تاریخِ اشتباهِ بازنشستگیِ مدل به پنج سند تکثیر شد و یک فوریتِ ساختگی ساخت که
چند تصمیم را هدایت کرد. خطا در استدلال نبود، در **منبع** بود؛ فکرِ بیشتر روی منبعِ غلط
فقط نتیجه‌ی غلطِ **مطمئن‌تر** می‌سازد.

### ۱۵-۷) لاگ و خطا
- خطاهای عملیاتیِ مهم با پیشوندِ ثابت و قابل‌grep چاپ شوند (`❌ GLOBAL`,
  `❌ UNHANDLED_REJECTION`, `❌ UNCAUGHT_EXCEPTION`).
- مالک غیرفنی است و **لاگِ verbose تنها راهِ او برای گزارشِ خطا به توست.** لاگِ ساختاریافته
  و پرجزئیات پیش‌فرض باشد، نه چیزی که بعداً اضافه شود.
- هر فراخوانیِ API خارجی: `try/catch` + retry + پیامِ خطای معنادار.

---

## ۱۶) گردش‌کار توسعه و «تعریفِ تمام‌شدن»

**گردش‌کار:**
1. برنچ `claude/<موضوع>` → کامیت با استانداردِ **Conventional Commits** → PR (draft).
2. سبزشدن CI.
3. **merge را خودِ سشن انجام می‌دهد** (خروج از draft + merge با GitHub MCP). از مالک
   نخواه merge کند.
4. رصدِ ورک‌فلوی Deploy → `Ops → status` → اگر لازم بود از مالک بخواه مسیرِ اصلیِ محصول
   را دستی تست کند → کمی بعد `Ops → errors`.
5. مشکلی دیدی: فوراً `git revert` و merge. رول‌بک همیشه بهتر از دیباگِ روی محصولِ زنده است.

**قاعده‌ی «تعریفِ تمام‌شدن» — PR بدون این‌ها ناقص است:**
- هر PR که **رفتار** را عوض می‌کند (قیمت، فلو، جدولِ DB، پرامپت، دستورِ ادمین، env جدید)
  باید `CLAUDE.md` همان پروژه را هم به‌روز کند.
- هر PR رفتاری باید **مسیرِ برگشتِ بدون فکر** داشته باشد: revert، یا یک فلگِ بولیِ
  یک‌خطی. رول‌بکی که فکر لازم داشته باشد، وسطِ خرابی انجام نمی‌شود.
- تغییرِ رفتاریِ رو-به-کاربر = بامپِ یک شماره‌ی نسخه‌ی محصول در همان PR، تا بعداً بشود
  فهمید هر کاربر با کدام نسخه وارد شده.

**اگر محصول کاربرِ واقعی دارد (زنده است):**
- تغییرها را در یک PR جمع کن و در ساعتِ کم‌ترافیک merge کن. چند merge پشت‌سرهم روی یک
  محصولِ زنده در ساعتِ اوج ممنوع (هر دیپلوی = ری‌استارت = مرگِ فلوهای درجریان).
- پرداختِ pending با قیمت و شرایطِ قبلی باید معتبر بماند (قیمت را از رکورد بخوان، نه از
  ثابتِ لحظه‌ی تأیید).

---

## ۱۷) سؤال‌های اجباری

جوابشان لازم نیست «بله» باشد، ولی **باید پرسیده و آگاهانه جواب داده شوند** و جواب در
`CLAUDE.md` پروژه ثبت شود.

**قبل از ساخت:** کاربر در چند ثانیه به اولین ارزش می‌رسد؟ اگر پولی است، پی‌وال کجاست و
چرا آنجا؟ چه چیزی کاربر را برمی‌گرداند؟ چرا کاربر به این محصول اعتماد کند؟

**قبل از merge:** اگر پروسه همین الان ری‌استارت شود چه گم می‌شود؟ اگر سرویسِ خارجی بعد
از همه‌ی فالبک‌ها جواب ندهد چه؟ اگر این دکمه دوبار یا دیر زده شود چه؟ اگر ورودی خصمانه
باشد (فایلِ غول، متنِ تزریقی، حلقه‌ی درخواست) چه؟ کدام مسیرِ رایگان هنوز بی‌سقف است؟
داده‌ی حساسِ جدیدی ذخیره می‌شود و چقدر باید بماند؟ `CLAUDE.md` به‌روز شد؟

**قبل از انتشارِ عمومی:** همه‌ی سقف‌ها ست‌اند و پیامِ برخورد به سقف مؤدبانه است؟ آخرین
اجرای بکاپ سبز است؟ هشدارهای سلامت و تلگرام واقعاً **تست** شده‌اند (نه فقط نوشته شده)؟
راهِ تماسِ پشتیبانی در پیام‌های مالی هست؟ برای توقفِ اضطراری برنامه داری؟

---

## ۱۸) اسکلت `CLAUDE.md` ریپوی جدید

```markdown
# CLAUDE.md — <نام پروژه>

> قبل از هر کاری `PLATFORM.md` را کامل بخوان — زیرساخت مشترک (سرور، کلیدها،
> دیپلوی، پایش، ممنوعه‌های سرورِ مشترک) آنجاست و در این فایل تکرار نمی‌شود.

## ۱) این پروژه چیست
هدف در یک جمله: ...
جنس محصول: تجربه‌محور یا ابزاری؟ ...
استیت: 🧪 تست شخصی | 🟢 زنده (کاربر واقعی دارد) | 🗑 در حال حذف

## ۲) معماری
- نقطه‌ی ورود: `index.js`
- اپِ pm2: `<PROJ>` · پوشه روی سرور: `~/<PROJ>`
- دیتابیس: `data/*.db` (SQLite)
- پورت (اگر وب دارد): `127.0.0.1:<port>`
- سرویس‌های خارجی: OpenRouter (کلیدِ `<PROJ>_OPENROUTER_KEY`) · تلگرام · ...

## ۳) کلیدها
| Secret | لازم؟ | کاربرد |
|--------|-------|---------|

## ۴) جدول‌های دیتابیس و معنی هر ستون

## ۵) فلوهای اصلی محصول

## ۶) تصمیم‌های محصولی و جواب سؤال‌های بند ۱۷ PLATFORM.md

## ۷) نسخه‌ها و تغییرات رفتاری
PRODUCT_VERSION = '1.0.0'

## ۸) کارهای باز و بدهیِ فنیِ شناخته‌شده
```

---

## ۱۹) عیب‌یابی سریع

| علامت | محتمل‌ترین علت | کار |
|-------|-----------------|-----|
| دیپلوی سبز شد ولی اپ بالا نیامد | سکرت در `env:` هست ولی در `envs:` نیست (قاعده‌ی ۵ بند ۵) | لاگِ دیپلوی را ببین: باید نامِ سکرتِ خالی چاپ شده باشد |
| هر مرج، حتی مستنداتی، اپ را ری‌استارت می‌کند | پینگ‌پنگِ `.env` (قاعده‌ی ۲ بند ۵) | ببین چه چیزی بعد از ساختِ `.env` به آن اضافه می‌شود |
| اپ بعد از ری‌بوتِ سرور بالا نیامد | `pm2 save` زده نشده یا وضعیتِ stopped ماندگار شده | `Ops → status` (خطِ `pm2 startup`) و بعد یک دیپلوی |
| `npm ci` روی سرور شکست خورد | سرور به registry نرسید | دوباره دیپلوی کن؛ تورِ ایمنیِ قاعده‌ی ۸ باید اپِ قبلی را بالا نگه داشته باشد |
| اپ `online` است ولی محصول کار نمی‌کند | خطای منطقی؛ پایش این را نمی‌گیرد | `Ops → errors` |
| هشدارهای تکراری و بی‌ربط می‌آیند | گاردِ پرسروصدا | همان لحظه شرطش را دقیق کن یا حذفش کن (بند ۸) |
| کوئری دیتابیس با اطمینان صفر ردیف می‌دهد | فایلِ دیتابیسِ اشتباه انتخاب شده | نامِ فایل را صریح بده، به «اولین فایل» تکیه نکن |
| دیسک پر شده | لاگ یا بکاپِ محلی | لاگ‌های قدیمیِ `~/.pm2/logs` را بچرخان؛ بکاپِ محلی روی سرور نگه ندار |

---

## ۲۰) پیشنهادهای اجرانشده

این‌ها **پیشنهاد**اند و بدون تأییدِ صریحِ مالک اجرا نمی‌شوند:

1. **کلیدِ SSH جدا per پروژه.** فعلاً همه‌ی ریپوها همان `VPS_SSH_KEY` را دارند، یعنی هر
   ریپو دسترسیِ کاملِ کاربر `ubuntu` را دارد. کلیدِ جدا با یک راه‌اندازیِ یک‌باره ممکن است
   و باطل‌کردنِ دسترسیِ یک پروژه را مستقل می‌کند.
2. **رجیستریِ مشترکِ اپ‌ها روی سرور** (مثلاً `~/.platform-apps.json`) که ناظرِ سلامتِ
   مونوریپو هم بخواند، تا چکِ «اپ اصلاً در pm2 نیست» پروژه‌های جدید را هم بگیرد و هر
   پروژه لازم نباشد `health.yml` خودش را داشته باشد (صرفه‌جویی در سهمیه‌ی Actions).
3. **سقفِ اعتبار per کلیدِ OpenRouter** (اگر پنل چنین امکانی می‌دهد — در این سند
   **تأیید نشده**، باید در پنل دیده شود). فایده: یک حلقه‌ی معیوب در یک پروژه نمی‌تواند
   اعتبارِ همه‌ی محصولات را خالی کند.
4. **خواندنِ آدرسِ سرور از یک Secret** به‌جای نوشتنش در فایل، برای ریپوهایی که ممکن است
   عمومی شوند. (تصمیمِ فعلیِ مالک: نوشتنِ صریح، چون فایل باید خودکفا باشد.)
