# کپی به `.env` و پر کنید. هرگز توکن واقعی اینجا نگذارید — این فایل برای git است.
# WH_SECRET: یک رشتهٔ تصادفی طولانی خودتان (مثلاً خروجی: openssl rand -hex 32)
#   همان مقدار باید در Cloud Run هم ست شود و در setWebhook به‌صورت secret_token فرستاده شود.
BOT_TOKEN=
GEMINI_API_KEY=
OPENROUTER_API_KEY=
